# MEH-1907 · decision 4א — SUPERSEDED probe (scratch, do not merge)

This file exists only to make `pr-checks.yml`'s paths filter report
`frontend=true`, so the frontend legs actually run and can then be
cancelled by a second push. Reading `SUPERSEDED` out of the `CI gate`
summary of the cancelled run is the observation decision 4א needs and
has never had.

The PR carrying this file is closed and the branch deleted immediately
after the log is read. Nothing here is intended to land.
