# MEH-1907 · decision 4א — SUPERSEDED probe B (scratch)

Makes the paths filter report `frontend=true` so the frontend legs run and
can be cancelled by a second push. Probe A proved the mapping prints and is
not counted; its own gate still went red on an unrelated real failure, so
this run isolates the green case.

Closed and deleted as soon as the log is read.
