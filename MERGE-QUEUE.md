# MERGE-QUEUE — auto-batch pass (2026-06-16)

Triage of all Linear issues labeled **`auto-batch`** (Mehamakor team), ordered by
actionability. Sapir's merge checklist. Generated by an autonomous pass — **no
merges performed** (merge authority is Sapir's).

**Key finding:** none of the 11 currently-open PRs map to an auto-batch issue —
they are all dependabot bumps (#1122–#1131) + one `pr-checks.yml` patch (#1146).
So there is **no pre-existing auto-batch PR in MERGE-TRIAGE**. Two Backlog issues
were buildable this pass; the rest are Done or In-Progress in other sessions.

---

## A · Built this pass (review → merge)

| MEH | PR# | CI | mergeable | closes? | live-renders | verdict |
|---|---|---|---|---|---|---|
| MEH-288 | [#1160](https://github.com/levismadar80-ship-it/FoodMamkor/pull/1160) | green (after `parity` dual-branch fix `de5d209`) | yes | y | n/a¹ | **READY for Sapir review** — DRAFT, mobile QA pending |

¹ `live-renders` n/a: `/producer/dashboard` is auth-gated (producer role) + the card's
state depends on the producer's own data — SSR/HTTP can't exercise it. Interactive:
manual (Sapir mobile QA on the 4 states). Two rounds of automated review addressed
(ICU-plural grammar, hardened fallback, Link convention, JSDoc) — all resolved or
declined-with-reason in PR thread.

---

## B · Already done — no action

| MEH | Status | Evidence |
|---|---|---|
| MEH-806 | **Done / merged** | i18n `open_orders` fix shipped via #1152 (`b0136f5`) + #1157 (`9cf1b1d`); Linear status = Done. |
| MEH-203 (Phase A) | **Mockup already on staging** | `frontend/public/meh-203-mockup.html` committed via #1102 (`88fc892`) — complete (search + popular chips + "עוד" + dietary tags, RTL). Phase A deliverable satisfied. **Phase B (real component) STILL GATED on Sapir "go for code"** — do NOT implement. |

---

## C · Skipped — active in other sessions (collision risk, do NOT touch)

All are Linear **In Progress** with `startedAt` set by prior/parallel sessions, but
have **no open PR and no remote branch** for the remaining scope. Per the pass's
collision guard, left untouched.

| MEH | Pri | Title | Note |
|---|---|---|---|
| MEH-807 | Med | Home hero cold-gray diagnose/fix | Most-recent `startedAt` (2026-06-15 19:34); explicitly flagged active-elsewhere. |
| MEH-793 | Urgent | Remove `/neighbor` feature | Partially shipped (#1050 `06bb5c3` removed the route); still In Progress — remaining scope unclear, leave to owning session. Also a DNA-lock / legal-copy-removal change → HIGH-RISK gate. |
| MEH-233 | Urgent | Mobile-responsiveness audit | Ongoing audit; multiple follow-ups already merged (#1042/#1046/#1047). Audit-type, not a single build. |
| MEH-258 | High | SECURITY-CHECKLIST.md traps doc | In Progress, no artifact on remote. Security-doc — leave to owner. |
| MEH-800 | Med | `ui/Popover` primitive + BadgeRow migration | In Progress; central-component refactor — owning session. |
| MEH-798 | Med | Map category icons (legend + popup) | In Progress; `/map` (central component MapClient) — owning session. |
| MEH-785 | Low | CalendarView i18n (he-IL hardcoded) | In Progress, no artifact on remote. |

---

## Dependency / merge order for Sapir

1. **MEH-288 (#1160)** — independent; merge after mobile QA. Unblocks MEH-290 step 1.
2. Everything else — no merge action this pass (Done, or owned by other sessions).

## Triage'd findings filed this pass

None. (No net-new bugs discovered in the two buildable issues that warranted a
separate `auto-batch-finding` stub.)
