import * as React from 'react';

/**
 * HeroSearch — from mehamakor-frontend@1.0.0.
 */
export interface HeroSearchProps {
  [key: string]: unknown;
}

export declare const HeroSearch: React.ComponentType<HeroSearchProps>;
