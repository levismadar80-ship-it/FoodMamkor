# MEH-1171 — Conversion progress (checkpoint file)

> Updated after EVERY converted section (checkpoint-and-continue, per ticket).
> Matrix approved by Sapir 13/07 (incl. 138 STALE deletions, KEEP-RUNBOOK, 6 UNCLEAR resolutions).
> Ticket B (MEH-1176) ran first: F1-F10 → 8 fixed/merged, F6+F7 stopped (see matrix § Ticket B execution status).

## Status: 🚧 CONVERTING — checkpoint 13 (admin producer status labels, MEH-294) done + F14 finding filed. **NEW FINDING F14 (matrix §Findings):** the producer-detail page fails a CLUSTER of CI E2E tests against the real backend — existing `12-axe-a11y.spec.ts:85` + `parity.spec.ts:131` (both out-of-scope) AND the MEH-1171 `producer-detail-cta.spec.ts:39` mobile sticky-bar (preconditions pass but scroll behaviour fails). Reproduces ONLY in CI, never locally; not caused by this branch (zero app-code edits). Per the >2-attempt stop rule the mobile sticky-bar test is `test.fixme()`'d (kept, re-activates when the page is fixed); needs Sapir diagnosis. Also stabilized the CP8 multi-select flake (predicate-based request capture: wait for the request carrying BOTH params, not "the last one"). checkpoints 1-6 CI-verified, 7 CI-green (137 passed on a2bb613). **The producer-detail VRT baseline was regenerated on staging (d0560b44..1549dfe4) and merged into this branch at checkpoint 10** — the standing CI-E2E red leg (`parity.spec.ts:127`) should now clear; verify on the next E2E run.

- [x] Phase 0 report + full triage matrix (`docs/qa/manual-testing-matrix.md`, 1,074 rows) — merged in PR #1710
- [x] **Sapir approved matrix** (13/07)
- [x] `scripts/local-backend.sh` — VERIFIED WORKING in the sandbox: system Postgres 16 → ephemeral `mehamakor_local` → `alembic upgrade head` (full chain) → `seed_data.py` + `create_admin.py` → uvicorn; `GET /health` = `{"status":"ok","db_init":"ready"}`, seeded `/producers` served. `SKIP_UVICORN=1` = DB-prep-only mode for CI/runner-managed processes.
- [ ] Conversion, section by section (CONVERT-PW → `frontend/e2e/flows/manual/`, CONVERT-PYTEST → `tests/test_manual_*.py`) — **next: checkpoint 2**
- [ ] MANUAL_TESTING.md rewrite: pointer stubs (MEH-671 pattern) + unified Tier-3 checklist at top + STALE sections deleted + KEEP-RUNBOOK verbatim
- [ ] New suite green ×2 consecutive SCOPED runs (`npx playwright test e2e/flows/manual`), output pasted here
- [x] FINDINGS handed to Ticket B (F1-F12; F13 added during Ticket B)

## Conversion order (checkpoint 2+ plan, highest-value first per matrix)

1. Legal pages + static pages (/privacy /terms /accessibility content — spec 13 covers only /about+/events)
2. MEH-722 map legend (live feature, zero coverage)
3. Mobile /map near-me (07-gps-button skips mobile entirely; PW geolocation mocks)
4. CitySearch autocomplete (stubbed everywhere, no dedicated test)
5. MEH-853 upgrade-path submit body (vitest gap → PW or pytest per item) ✅ checkpoint 7
6. CONVERT-PYTEST cluster (23 items) against the local backend ✅ checkpoint 6
7. Remaining CONVERT-PW sections in matrix order; destructive ones via scripts/local-backend.sh ⬅ next

## Per-section log

| Section | Items | Target file | Status |
|---|---|---|---|
| (infra) local backend runbook | — | `scripts/local-backend.sh` | ✅ verified (health 200 + seeded reads) |
| CONVERT-PYTEST cluster (cross-section, 23 matrix rows) | 15 new tests covering 13 rows; 7 rows already covered by the Ticket B files (OTP limits → test_meh1176_otp_confirm_rate_limit.py; LIKE literals ×2 → test_meh1176_like_escape.py; experience emails ×2 → test_meh1176_experience_email_notifications.py; cite in stubs); chat grep-items 12+13 stay doc grep-guards; 1 row DOC-STALE (analytics POST 429 — endpoint retired, tracking moved server-side onto GET /producers/{id}, covered by test_analytics.py) | `tests/test_manual_backend.py` | ✅ **15 passed ×2** (10.2s / 7.7s) + `pytest tests/test_api.py` 206 passed. Covers: MEH-530 admin-pending license payload · MEH-1011 owner license renewal PUT · MEH-293 is_vegan POST/PUT persistence · /contact 5/hour → 6th 429 · producer_page_views hash-only invariant (inspector) · chat KB facts (5 items incl. both-sides "העסק שלך") · /search 60/min → 61st 429 · MEH-287 skip-log at ERROR level · organic filter + city composability. |
| Map city search width + dropdown z-index | 6 of 7 items → PW (items 1-4, 6-7; item 5 mobile-w-full = covered by the functional mobile run) | `frontend/e2e/flows/manual/city-search.spec.ts` | ✅ green ×2 (28 passed / 28 passed suite-wide, desktop+mobile). First dedicated CitySearch coverage (matrix flagged: reused 3+ surfaces, stubbed everywhere). Occlusion items asserted functionally — Playwright hit-testing fails on covered targets, so a successful option click proves the listbox paints above the Leaflet panes. Wizard regression (item 7) rides spec-18's testid walk. **Infra:** `local-backend.sh` §4b now seeds a deterministic 6-city set (the cities table was empty — seed_data.py never covered it; the full loader needs data.gov.il egress). **F13 addendum (new sibling):** `backend/app/routers/cities.py:33` builds an UNESCAPED prefix ILIKE (`f"{clean}%"`) on a PUBLIC endpoint — missed by the F1 sibling grep because the pattern shape differs (`f"%{` vs `f"{`); same fix class as F1/F13, logged not fixed (zero-app-fixes). **Mock note:** the legal-pages contact test + this spec's register walk use route mocks for WRITE avoidance only — same precedent as spec 18's own register-POST mock; reads stay real per MEH-417. |
| /map near-me mobile (MEH-970 2-lite) | 4 of 4 items → PW (mocked geolocation: grant-near / grant-far-25km / deny) | `frontend/e2e/flows/manual/map-near-me.spec.ts` | ✅ **green ×2 locally (20 passed / 20 passed suite-wide)** after the CI FAIL was root-caused. **Correction of the earlier note:** the "sandbox can't mount mobile /map" claim was WRONG — the real causes were (1) the MEH-549 two-instance race (hidden desktop + visible mobile MapPane both mount; visibility-waits on `.leaflet-container` resolve to the hidden one — the race-free mount signal is `window.__MAP_CENTER__`, REUSES 05-map-navigation) and (2) the pill's accessible name is its aria-label "הצגת בתי עסק קרובים למיקום שלי", not the visible "קרוב אליי". **CI-VERIFIED ✅** — run 29249887485 (commit 5473b86): all 4 near-me mobile tests passed on the runner; that run's only failure is the pre-existing producer-detail VRT baseline drift (non-required, baseline-regen follow-up, untouched by this branch). |
| /map legend (MEH-722) | 3 of 4 items → PW (items 2,4 live; items 1+3 = test.fixme) | `frontend/e2e/flows/manual/map-legend.spec.ts` | ✅ suite green ×2 (16 passed / 16 passed incl. legal-pages). **Runtime-verified semantics documented in the spec header:** the legend is a MULTI-TOGGLE (neutral = all rows aria-pressed=true; a click EXCLUDES a category), map interaction collapses the legend, and counts key off committedBounds (the "חפשו באזור זה" pill commits). **Deferred (fixme):** items 1+3 need a deterministically empty committed viewport — mouse-drag panning kept catching seeded businesses in the sandbox (stopped after 2 attempts per the stop rule); needs a map test hook or geo URL params. **UX quirk noted (not fixed — zero-app-fixes):** excluding an EMPTY category disables its own row, so it can only be recovered via "הציגו הכל". |
| Legal pages (אפריל 2026) | 8 of 14 → PW (items 1,2,3,8,9,10,11,13) | `frontend/e2e/flows/manual/legal-pages.spec.ts` | ✅ green ×2 (14 passed / 14 passed, desktop+mobile) vs live local stack. Items 4-7 = pytest/Tier-3 (email inbox = Tier-3; DB row + 429 + fail-open = existing pytest, cite in the MANUAL_TESTING stub). Item 12 = pending coverage check vs 18-producer-register-wizard. Item 14 = STALE (approved). **Doc-stale notes:** contact toast copy is now `contact.success_title` "תודה! קיבלנו את הפנייה." (emoji removed, Emoji LOCK v2); footer ships 3 legal links — the checklist's 4th (קשר) was dropped in the footer redesign; accessibility date line is "עודכנה לאחרונה", not "עדכון". |
| Producer status labels (MEH-294) — admin table chips | 1 PW test covering items 1-5 (each status → translated chip + status-specific bg: pending_whatsapp→bg-orange-100, pending→bg-yellow-100, approved→bg-primary, rejected→bg-red-100, inactive→bg-gray-200) + item 6 (labels translated, never a raw code). Item 8 (unknown-status raw-code fallback) NOT converted — the `status` column is enum-constrained (can't seed a bogus value); the `getProducerStatusLabel(x) ?? x` fallback is unit-territory. | `frontend/e2e/flows/manual/admin-status-labels.spec.ts` | ✅ green ×2 (1 passed / 1 skipped — desktop-only, admin-auth-gated). **First admin-authed manual spec — establishes the reusable admin pattern** for the many remaining admin items: REUSES 20-admin-recipes-queue's `SMOKE_ADMIN_EMAIL`/`SMOKE_ADMIN_PASSWORD` env gate (skips if unset — CI sets them or skips) + browser-side `/api/auth/login` with `credentials:"include"` (the MEH-327 fingerprint/refresh cookies must land in the browser jar — a bearer-only token 401s; confirmed via curl) + desktop-project-only. Per-status find-or-skip (assert whatever statuses the table carries; ≥1 required). **Must wait for the GET /admin/producers fetch to populate before asserting** — the empty state ("אין בתי עסק להצגה") renders until rows land. **`local-backend.sh` §4d (NEW):** CREATES 4 dedicated non-approved status-fixture producers (`status-fixture-*`) rather than reassigning the seed producers' status — because §4c (CP12) needs producers 0-2 to stay `approved` (public detail pages 404 for non-approved, and the `?availability_state=` filter excludes them). The 4 fixtures are non-approved so they stay OUT of the public list CP12 queries → the two suites don't interact (verified green together). |
| Producer detail STATE surfaces — MEH-291 availability banners (items 14/15/23) + MEH-51 kashrut (items 5/6/7) | 4 PW tests: on_vacation → vacation banner + return line; full_this_week → slow-response banner; kashrut_badges present → KashrutBadgeStrip label (+ near-expiry warning when expires ≤30d); empty kashrut_badges → no strip. MEH-51 item 4 (verified seal / BadgeRow) deferred (its own surface); admin/dashboard availability controls (MEH-291 1-4/16-19) are auth-gated → separate cluster. | `frontend/e2e/flows/manual/producer-detail-state.spec.ts` | ✅ green ×2 (8 passed / 8 passed). **DATA-DRIVEN find-or-skip:** each test QUERIES the live `/api/producers` (with `?availability_state=` filter, which the list endpoint supports) for a producer carrying the attribute and skips gracefully if none — meaningful locally, CI-safe against the real Railway backend where the exact producers differ (same philosophy as 03's empty-DB skip). Reads real (MEH-417). **Local fixtures:** `scripts/local-backend.sh` §4c (NEW) stamps 3 seed producers with on_vacation / full_this_week / verified+badatz(near-expiry) — the default seed is all accepting_orders/unverified/no-kashrut so these surfaces never render otherwise. **ISR gotcha (real, fixed):** the `[slug]` detail page is ISR-cached (`revalidate: 60`, page.js:16) — a producer whose data changed within 60s serves one stale render before the background revalidation lands. `gotoWithMarker()` reloads up to 4× to ride out the window; a no-op single nav in CI (staging data long-stable), only matters right after a local re-seed. Does NOT mask a bug — a marker that never appears still fails. Verified-tier now = `verified_at`→computed `verification_tier` (is_verified DROPPED, MEH-766); kashrut codes map via CODE_TO_KEY (badatz→בדצ"ה). |
| Producer Detail Page (redesign) | 2 PW tests covering the CTA-placement invariant: item 3+4 (mobile StickyContactBar parks off-screen at top → slides in past the inline CTA → slides out on scroll-back) and item 8 (desktop mounts exactly ONE visible primary CTA — sidebar only; the `lg:hidden` inline card + sticky bar are hidden). 03-view-producer-detail covers only h1+one-CTA — untouched. Remaining rows (5 vacation-state, 7 eager-image, 9 reviews-lazy-IO, 11 RTL dates, 12 contact_name, 13 highlight chips) need attribute-rich seed fixtures → separate follow-up cluster. | `frontend/e2e/flows/manual/producer-detail-cta.spec.ts` | ✅ green ×2 locally (2 passed / 2 skipped each). Real reads; navigates via card-click (seed-agnostic, like spec 03). **Note:** the sticky bar hides via CSS `transform`, not `display` — Playwright's `isVisible()` stays true, so viewport-position (`toBeInViewport()`) is the correct visible/hidden signal. testids: `sticky-primary-cta` (bar), `primary-contact-button` (ContactCard, inline+sidebar). **CI-HARDENING (post-first-CI-run):** CI runs against the REAL Railway backend where the first producer is arbitrary — the mobile sticky-bar test failed on 50aff3a because the sticky-bar contract only holds for a producer WITH a contact method AND a page tall enough to scroll past the inline CTA. Both tests now precondition-gate (skip gracefully if no CTA / page too short), same philosophy as 03-view-producer-detail's empty-DB skip — NOT assertion-weakening (a contactless/short page has no sticky-bar contract). **OBSERVATION (not caused by this branch — zero app-code edits):** on the same CI run the EXISTING tests `e2e/flows/12-axe-a11y.spec.ts:85` (critical/serious a11y on /producer/[id], desktop+mobile) AND `e2e/visual/parity.spec.ts:131` (producer detail, 42% pixel diff) also failed — the producer-detail page renders materially differently against the current real backend/preview. This is a real signal for Sapir (possible staging regression or stale/broken preview + VRT baseline mismatch), OUT of MEH-1171 scope (existing non-required tests, real backend). Surfaced, not silently absorbed. |
| Content pages — /about/process (MEH-534) + /join (MEH-995) + /share (MEH-1160) | 10 PW tests batched into one spec. process: items 1 (tab title), 2 (hero "היכרות אישית"), 4 (3 cards זהות/סיפור/שיחה), 9 (CTA→/register/producer). join: items 1+6 (hero "העסק שלכם. עמוד משלו." + single "מצטרפים" CTA→wizard). share: items 1 (4 actions), 2 (wa.me prefill), 5 (mailto subject), 3 (copy toast "הקישור הועתק"). footer: MEH-534#10/995#7/1160#6 (all three footer links, asserted once on the homepage `contentinfo`). Remaining rows = RTL/visual/tap-target (VRT territory) or DEVICE-ONLY (native share sheet, matrix Tier-3). | `frontend/e2e/flows/manual/content-pages.spec.ts` | ✅ green ×2 (20 passed / 20 passed) + full manual suite green (78 passed / 8 skips). Static SSG reads only. **Notes:** /share renders responsive desktop+mobile action rows → scope every share-testid to `.locator("visible=true")` (same two-instance class as hero-search/MapPane); copy test grants `clipboard-read/write`. **Verbatim-copy corrections vs checklist:** footer /join link is "הוסיפו את העסק שלכ**ם**" (checklist wrote שלך). |
| Smart Search — HeroSearch + /producers?q= (MEH-99) | 9 PW tests covering items 1 (recent dropdown+click), 2 (trending), 3 (single-char guard), 4 (grouped autocomplete), 5 (keyboard ArrowDown+Enter), 7+8 (recent-persist + debounce), 10 (results heading), 13 (empty state). Item 12 (bold highlight) = COVERED unit (highlight.test.jsx); item 9 (AbortController) folded into the debounce assertion (impl-detail, not separately observable). Existing 02-search-producer covers only basic submit-nav — untouched. | `frontend/e2e/flows/manual/smart-search.spec.ts` | ✅ green ×2 (16 passed / 16 passed, desktop+mobile). **Real reads (MEH-417)** of GET /search, /producers?q=; ONE documented mock: `/search/trending` CONTENT is stubbed for the trending-wiring test only (it's a server aggregate of search history — empty on a fresh DB, non-deterministic — so mocking the content makes the render assertion deterministic; the WIRING is what's under test). **Robustness notes:** the homepage renders TWO `hero-search` inputs (responsive desktop+mobile wrappers, one visible per breakpoint) → scope every interaction to `.locator("visible=true")` (same class as the /map two-MapPane race); a `waitForURL` regex must not accidentally match the domain (`//localhost`) — assert on `new URL(url).pathname` against `^/(he\|en)?/?$`. Debounce asserted by counting /api/search requests during a rapid keystroke burst (≤2 for 7 chars). **Doc-stale note:** item 13's checklist string "לא נמצאו בתי עסק" is the COUNT line; the empty-state heading is `empty.no_match_search` = "לא מצאנו בתי עסק עבור {q}" — assertion follows the shipped copy. |
| Advanced filter chips — homepage (task 12) | 4 PW tests covering items 3 (scrollable track), 4+5 (inactive/active state via aria-pressed), 6 (kosher→param), 7 (deactivate drops param), 8+9 (multi-select AND params). Items 10/11/13/19/20 = COVERED (unit: useHomePageDietChipsUrl / HomeCategoryGridLinks / mapChips) or /map-specific (map-legend territory); item 1 heading/chip-presence = COVERED he.json:430 + CHIPS_CONFIG. | `frontend/e2e/flows/manual/home-filter-chips.spec.ts` | ✅ green ×2 (8 passed / 8 passed, desktop+mobile). First end-to-end drive of the REAL wired chip flow: click → live GET **/api/producers** fires with the right query param → aria-pressed flips. The param-mapping was unit-covered but the wired flow was untested. **Robustness notes (2 real races fixed, documented for reuse):** (1) Next.js fires RSC route-prefetches to the /producers PAGE (`/producers?category=N&_rsc=`) that must NOT be mistaken for the axios `/api/producers` chip call — match `/api/producers` only; (2) the initial page-load `/api/producers` can fire late and be captured instead of a click's request → track ALL api/producers requests and read the LAST after each action settles, gated on the producers-counter for initial-load settle. Multi-select needs an aria-pressed-committed wait between clicks (React batches the toggle state). Visual items (chip colors) asserted via aria-pressed = functional equivalent; pixel colors are VRT territory. Reads real (MEH-417, observe-not-mock). |
| MEH-853 — /register/producer frame 01 (DETAILS): city+address (upgrade path) | 3 PW tests covering items 1 (city ✕ clear), 4 (upgrade submit body), 5 (auth lands on DETAILS). Items 2,3 = COVERED (RegisterProducerClient.test.jsx submit-body vitest); item 5's other two freeze legs (declaration gate :199, license field :253) = COVERED vitest; the OAuth/authenticated-lands-on-DETAILS leg was the untested one → converted here. | `frontend/e2e/flows/manual/register-wizard-upgrade.spec.ts` | ✅ green ×2 (34 passed / 34 passed suite-wide incl. all prior checkpoints, 8 documented skips: map-legend fixmes ×2 projects + desktop/mobile-only guards). Closes the matrix gap: the shared-submit-body-above-!isUpgrade claim (item 4) was trust-the-code; now the UPGRADE POST is captured and asserted to carry city+address AND to OMIT email/name/password (MEH-143). **Mock note:** auth (`/auth/me`), `/categories`, and the register POST are route-mocked for WRITE avoidance (destructive registration → local-only per Sapir 13/07; same precedent as spec 18); `/users/me/favorites` also mocked empty so the MEH-156 401-interceptor can't null the mocked session mid-test. `/cities` reads stay real. **Zero-app-edits caveat:** the didUpgrade success screen carries no `data-testid` (the confirm testid is the non-upgrade branch only), so the upgrade branch is asserted by its heading "קיבלנו את הפרטים שלך" — which is the STRONGER check (proves the access_token→didUpgrade branch rendered, not just any confirm). |

## Runtime notes for the next checkpoint

- Sandbox Playwright: launch with `executablePath: "/opt/pw-browsers/chromium"` (project pin wants a browser build absent from the sandbox cache; do NOT `playwright install`).
- Playwright route-mocks: catch-all route must be registered FIRST (last registered wins).
- Frontend for live checks: `npm run build && npm run start` (`/api/*` proxies to `http://localhost:8000` by default — pairs with local-backend.sh).
- Verbatim-copy caution: several checklist strings predate later copy changes (e.g. Emoji LOCK removals) — runtime-verify each assertion string against the live app; checklist-vs-app copy mismatch = doc-stale note in the section log, not a test weakening.
