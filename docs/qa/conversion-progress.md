# MEH-1171 — Conversion progress (checkpoint file)

> Updated after EVERY converted section (checkpoint-and-continue, per ticket).
> **Conversion has not started ON `staging` — but it is NOT unstarted.** A body of converted work exists,
> unmerged, on `origin/feature/meh-1171-manual-testing-conversion` @ `3390c612`. See § Prior work below
> BEFORE writing a single spec; starting from zero here rewrites ~90 existing tests.
>
> Nothing blocks the conversion. Both preconditions cleared 05/08; what remains is queue order, not a gate
> (MEH-1249: after MEH-1909 — which closed 16/08 — and launch-blocking items).

## Status: ▶️ READY — both preconditions cleared, conversion may begin

Unblocked by Sapir's ruling of **05/08** (recorded in full on MEH-1249): option **(ב)** — a
fresh branch off `staging` plus a copy of the 17 net-new files, rather than reviving the
1,295-commit-behind `feature/meh-1171-manual-testing-conversion`. The ruling's wording:
*"הכרטיס משוחרר לריצה"*. Its first caveat — `scripts/local-backend.sh` ships first, in its
own PR, with a proven run — is satisfied (see below).

Branch to cut: `feature/meh-1249-manual-testing-conversion` off `staging`. Do **not** carry
`HANDOFF.md` across (rule 31 — `changelog-branch-guard` reds a code branch that touches it).

- [x] Phase 0 report + full triage matrix (`docs/qa/manual-testing-matrix.md`, 1,074 rows — this line read "1,068" until 04/09; the matrix's own header and a row count both say 1,074)
- [x] **Sapir approves matrix (incl. STALE deletions)** — DONE 05/08, ruling on MEH-1249
- [x] `scripts/local-backend.sh` (uvicorn + ephemeral Postgres + alembic upgrade head + seed) — DONE, merged in PR #2621 (`cc8c72b1`, on `staging`)
- [ ] Conversion, section by section (CONVERT-PW → `frontend/e2e/flows/manual/`, CONVERT-PYTEST → `tests/test_manual_*.py`)
- [ ] MANUAL_TESTING.md rewrite: pointer stubs (MEH-671 pattern) + unified Tier-3 checklist + STALE sections deleted
- [ ] New suite green ×2 consecutive runs (anti-flake), output pasted here
- [ ] FINDINGS handed to Ticket B

## Chunk plan (04/09)

Sapir's ruling for stage 2 (MEH-1249, 04/09): **"refresh scope (1,654), one page per chunk per PR."** The
scope is the *current* `docs/MANUAL_TESTING.md` (1,660 items on `staging` on 04/09 — the ruling's 1,654 was
the 01/09 count), the frozen matrix stays the SoT for the 1,074 rows it covers, and the items it does not
cover get their verdict **inside the page chunk that touches them** — same 7-column schema, appended to
the matrix, never regenerated. Each chunk = one app **page** = one PR.

Section → page assignment and every count below come from `docs/qa/conversion-page-map.md`, generated by
`scripts/qa/page-map-manual-testing.py` (204 `##` sections · 1,660 items · 1,060 with a matrix row · 600
without). `MANUAL_TESTING.md` is organised by ticket, not by route, so the page is a recorded judgement
per section, not a lookup — see the map's header for the rule and its two caveats (line-number-keyed
tier sidecar; unlisted heading fails the generator).

| chunk | page | sections | items | with matrix row | without | status |
|---|---|---|---|---|---|---|
| 0 | this plan + `conversion-page-map.md` + tier sidecar re-run | — | — | — | — | in PR |
| 1 | `/share` | 1 | 6 | 6 | 0 | in PR |
| 2 | `/join` | 1 | 8 | 8 | 0 | not started |
| 3 | `/about/process` (+ `/about`, `/about/why-local`) | 4 | 28 | 16 | 12 | not started |
| 4 | legal — `/privacy` · `/terms` · `/contact` · `/accessibility` | 1 | 14 | 14 | 0 | not started |
| 5 | `/producers` | 9 | 70 | 18 | 52 | not started |
| 6 | `/map` | 14 | 96 | 75 | 21 | not started |
| 7 | `/` (home) | 21 | 184 | 137 | 47 | not started |
| 8 | `/producer/[id]` | 26 | 203 | 87 | 116 | not started |
| 9 | `/register/producer` | 13 | 89 | 33 | 56 | not started |
| 10 | `/login` + `/register` | 9 | 85 | 80 | 5 | not started |
| 11 | `/producer/dashboard/*` | 39 | 327 | 161 | 166 | not started |
| 12 | `/admin/*` | 13 | 233 | 170 | 63 | not started |
| — | cross-cutting / not a page (component tests, emails, backend-only, CI, runbooks, other routes) | 53 | 317 | 255 | 62 | no chunk — triaged where its rows already are; a page chunk that meets one of these sections re-homes it in the map |

Every chunk PR that edits `MANUAL_TESTING.md` re-runs **both** generators (`tier-manual-testing.py`,
`page-map-manual-testing.py`) — the tier sidecar is keyed by line number and goes stale on the first
edited line.

## Per-section log

_(populated during conversion; one line per section: section name · items converted · spec file · run result)_

| Section | Items | Target file | Status |
|---|---|---|---|
| `MEH-1547 — מונה "+N" של תגיות בכרטיס עסק = disclosure לחיץ (26/07)` (chunk 5 — `/producers`, 04/09) | 7 rows · **7 converted** — rows 6+7 (max-2 lock + `aria-label` from `he.json`) in one test, row 7's EN twin on `/en/producers`, rows 1/2/3 in the mobile bottom-sheet test (sheet = `role=dialog` + `aria-modal`, N `listitem`s whose labels + tooltips are DERIVED from `lib/badges.js allBadges(...).slice(2)`, hit-test proves the sheet is topmost above the BottomNav, URL unchanged on tap, backdrop TAP closes), row 4 in the desktop panel test (panel bottom ≤ strip top, gap ≤ 40px, Esc closes + focus returns), row 5 (name link → `detailPath(...)`, URL only — the detail route is chunk 8's) | `frontend/e2e/flows/manual/producers.spec.ts` | green ×3 (see the chunk-5 note) |
| `MEH-1592 — ה-Popover של "+N" לא מתנגש בתגיות שכנות (26/07)` | 6 rows · **5 converted** — rows 1/2/3 in the desktop panel test (no `boundingBox` overlap with the badge strip / name / rating; horizontally over its own card; whole inside the 1440 viewport on BOTH edge columns of `xl:grid-cols-4`), row 4 (badge bubble ↔ +N: opening one closes the other, exactly one `[role=tooltip]`), row 6 in the mobile sheet test · **1 STALE, left untouched** — row 5 «the panel stays attached while scrolling» is SUPERSEDED by MEH-1871 (the panel now CLOSES on scroll, `Popover.jsx:169-216`) | same | green ×3 |
| `MEH-1465 — בחירת קטגוריות מרובה (OR) בשורות הצ'יפים (22/07)` | 9 rows · **3 converted** — row 2 (re-tap drops only its id — URL `getAll("category")` → `["1"]`, a refetch with only that id, «הכל» clears the set), row 3 (no category × on the control line; «נקו הכל» resets category AND attribute — env-neutral final check: «הכל» is the only `aria-pressed` chip), row 4 (deep link hydrates: the mount fetch carries repeated `?category=` and the grid is the fixture) · **row 1 partially asserted, left unchecked** — the union STATE (both pressed, `?category=1&category=2`, «הכל» ghost `bg-white`) is asserted via the deep link; the tap-to-ADD step needs an INACTIVE second chip, which the sandbox cannot render (`ProducersClient.jsx` `visibleCategories` hides a category with 0 loaded producers once the catalog is fully loaded) — unit-covered `__tests__/ProducersClientCategoryAxis.test.jsx:259` · **rows 5-6 COVERED (unit)** — `__tests__/ProducersClientSort.test.jsx:135/:147/:158`; the sort select is gated OFF below `RATING_SORT_THRESHOLD` (`page.jsx:70-76`, fail-closed with no backend) so it cannot be asserted deterministically here · rows 7-9 are `/map` → chunk 6 | same | green ×3 |
| `MEH-1452 — tint אייקון קטגוריה בצבע הקטגוריה (צ'יפים, inactive בלבד) (22/07)` | 6 rows · **2 converted** — row 3 («ביצים»: a glyph with NO `CATEGORY_STYLES` row → glyph colour === button colour, ring = `DEFAULT_CAT_RING`), row 4 («הכל» has no glyph span) · **row 2 STALE/drift, left unchecked** — it says a selected chip goes green with a WHITE glyph; since MEH-1465 / MEH-1181-A "Direction A" a SELECTED category chip carries the category colour as a 1.5px ring + wash and the glyph KEEPS its tint (`ChipScrollRow.jsx` `isCategorySelected`, `__tests__/ChipScrollRow.test.jsx:225`) — the spec asserts the live behaviour against the registry value (`#c04040` on `border-top-color` AND on the glyph, label NOT tinted) · row 1 COVERED `__tests__/ChipScrollRow.test.jsx:217` (an inactive category is visible only with SSR data) · row 5 visual residual (no before-state to compare) · row 6 COVERED `ChipScrollRow.test.jsx:236` + the sheet pills carry no `iconColor` · **drift found, not fixed:** row 1 quotes שמנים `#bd8013` / דבש `#C8821E` as the chip tint; the registry tints are `textColor` `#97690f` / `#A8681A` (`lib/category-registry.js:26/:32` — the pin colours stay `#e8a020` / `#C8821E`) | same | green ×3 |
| `MEH-1438 — סינון "צמחוני" (is_vegetarian ברמת מוצר)` | 6 rows · **2 converted** — row 2 for the `/producers` surface only (FilterSheet diet group heading from `he.json`, `role=switch` order DERIVED from `FILTER_AXES` = טבעוני · צמחוני · ללא גלוטן · ללא לקטוז; the gated 5th chip may follow) — the בית / `/map` surfaces the row also names belong to chunks 7 / 6; row 5 (`?vegetarian=1` in the URL, `vegetarian=true` on the fetch, reload keeps `aria-checked` + the «· 1» count) · row 1 dashboard → chunk 11 · **rows 3-4 COVERED (pytest)** — `tests/test_dietary_filter.py:129` (vegetarian product) and `:143` (a vegan PRODUCT implies vegetarian); the business-level asymmetry is a pinned decision at `tests/test_meh1508_dietary_scope_filter.py:142` · row 6 `/producer/[id]` → chunk 8 | same | green ×3 |
| `MEH-1186 — היררכיה ויזואלית ב-/producers (שפה אחת לכל התנהגות)` | 6 rows · **4 converted** — row 2 (no «צפית לאחרונה» strip even with a SEEDED `recently_viewed` history, control reads the seed back), row 3 (counter — ICU plural resolved from `he.json` — + vegetarian × + search × + «נקו הכל» are descendants of ONE `producers-control-line`; exactly 3 buttons; clear resets the URL to empty), row 4 (fixture returns `[]` → `producers-filter-empty` with the locked h2/hint, the × chip + «נקו הכל» still present, the empty state's own button clears), row 5 (search form width == category row width, ±2px) · **row 1 drift, left unchecked** — the «סינון» micro-label was RETIRED by MEH-1862 (the attribute row moved into a FilterSheet behind a «סינון» button; `ProducersClient.jsx` "The MEH-1186 micro-label is retired here"); the spec asserts the «קטגוריה» label + the «סינון» button, he and `/en` (Category / Filters) · row 3's list also names «קטגוריה ×», which MEH-1465 removed (a category is never a removable tag) — asserted as rendered · row 6 home → chunk 7 | same | green ×3 |
| `MEH-51 — Trust Ladder + Kashrut Badges (PR #183)` | 18 rows · **0 converted here** — nothing in the section is CONVERT-PW + non-destructive on this route: rows 1-3 COVERED (matrix rows 582-584: `ProducerCard.test.jsx:350`, `badges.test.js:29`, `TrustBadge.test.jsx:29/:47`), rows 4-7 CONVERT-PW on `/producer/[id]` → chunk 8 (5-6 `destructive = yes`), rows 8-12 STALE (wizard OTP step gone), rows 13/15 `/admin/kashrut` → chunk 12 (`destructive = yes`), rows 14/16/17 COVERED `tests/test_trust_ladder.py:268/:238/:318`, row 18 CONVERT-PYTEST · the page map filed the section under `/producers` on the strength of rows 1-3 (ProducerCard), which were already covered | — | reported only |
| `MEH-1871 — פאנל overlay נסגר בגלילה (03/08)` | 5 rows · **4 converted** — rows 1+2 (desktop: a badge bubble and the +N panel both close on `window.scrollBy`, with a CONTROL that `scrollY` actually moved), row 4 in the mobile sheet test (the sheet SURVIVES the same scroll — sheet mode is not the overlay), row 5 in the desktop panel test (Esc / outside click / re-tap) · row 3 DEVICE-ONLY (rotation; the `resize` + `orientationchange` listeners are pinned by `__tests__/Popover.test.jsx:243`) | same | green ×3 |
| `MEH-1880 — שורת "פתוח להזמנות" על כרטיס העסק (04/08)` | 7 rows · **7 converted** — rows 1/2/3/4/5/7 in one fixed-clock test (`page.clock.setFixedTime` → Wed 2026-09-02 03:00 Asia/Jerusalem): the open card's window has TWO ranges that day (00:00-05:31, 06:00-13:00) so the line must say the CURRENT range's «5:31», not the day's last «13:00» — that is the discriminator the red control used; closed-between-ranges / no-window / on-vacation-with-open-window cards render NO `card-order-window` and keep their `location-line`; row 6 mobile (`white-space` ≠ nowrap, `text-overflow` ≠ ellipsis, `scrollWidth ≤ clientWidth`, box inside the card) · **drift found, not fixed:** row 7 quotes «05:31»; `lib/time-format.js:46` `humanTime` strips the leading zero → «5:31», asserted as rendered | same | green ×3 |

_Chunk 5 note (04/09): the placeholder row was replaced, as chunk 4's branch did; chunks 1–4 land their own rows via their PRs and whichever merges last reconciles this table. **Both generators (`tier-manual-testing.py`, `page-map-manual-testing.py`) were NOT re-run on this branch** — same reasoning as chunks 1–4. **Data decision (Phase 0, option 1):** `/producers` SSR-fetches its first page but is fail-open (`page.jsx:33-48` catch → `{ items: [], total: 0 }`; `fetchRatingSortEnabled` `:70-76` → false), so the shell renders with no backend; every data row deep-links a filter, which makes ProducersClient issue a CLIENT fetch on mount (`ProducersClient.jsx:318-327`) that `page.route("**/api/producers**")` serves from an 8-row fixture `.parse`d in `beforeAll` against `ListProducersProducersGetResponse` (+ `/api/categories` against `ListCategoriesCategoriesGetResponse`, `lib/generated/api.zod.js`); the clock is fixed per test. The MEH-1968 three conditions are stated in the spec header — conditions 1 and 2 hold mechanically; **condition 3 is argued** (the unmocked alternative needs a shared, time-dependent catalog state no seed provides — not a rate limiter) and is flagged for Sapir. **Run:** green ×3 locally (desktop + mobile; 33 passed + 5 project-gated skips each run — 19 `test(` blocks × 2 projects = 38; 46.2s · 46.1s · 44.7s) · shown failing first (MEH-1619): the MEH-1880 row-1 assertion broken to the day's LAST close («13:00») → exactly that test red on both projects, `Expected: "פתוח להזמנות · עד 13:00" / Received: "פתוח להזמנות · עד 5:31"`, 31 passed alongside; restored, then the three greens. **10 attribute-only testids** added in `frontend/components/ProducersClient.jsx`: `producers-search-form` (:728), `producers-category-row` (:759), `producers-category-label` (:760), `producers-control-line` (:842), `producers-results-counter` (:843), `producers-clear-all` (:900), `producers-grid` (:951), `producers-filter-empty` (:1006), `producers-empty-clear` (:1039), `producers-catalog-empty` (:1056). **Unexplained observation, reported not attributed:** under the Pixel-5 project a `locator.click()` (mouse) on the +N sheet's backdrop is reported by Playwright as intercepted by the page container and leaves the sheet open, while `page.touchscreen.tap` at the same point closes it and `document.elementFromPoint` names the backdrop in both cases (measured 04/09 with a throwaway probe); the mobile test taps. **Sandbox-only visibility:** an INACTIVE category chip never renders without SSR rows (`visibleCategories`), which is why MEH-1465 row 1 and MEH-1452 row 1 are not asserted end-to-end here._

## Prior work — 13 specs + 15 pytest tests exist unmerged. Read this before starting.

_Added 01/09 (MEH-1249, drain יד'). This file used to say "Conversion has NOT started" with a one-row
`not started` table. That is true of `staging` and false of the repository, and the difference is ~90 tests._

`origin/feature/meh-1171-manual-testing-conversion` @ `3390c612` is still on origin, unchanged, and is now
**2,613 commits behind `staging`** (the MEH-1249 description says 1,295 — measured 01/09). Sapir's 05/08
ruling is option **(ב)**: cut a fresh branch off `staging` and COPY these files across, do not revive the
branch. The growing gap strengthens that ruling rather than weakening it.

Counted directly from the blob, not from prose — `frontend/e2e/flows/manual/`:

| Spec | `test(` blocks |
|---|---|
| `content-pages.spec.ts` | 13 |
| `legal-pages.spec.ts` | 8 |
| `producer-detail-state.spec.ts` | 8 |
| `smart-search.spec.ts` | 8 |
| `producer-detail-cta.spec.ts` | 7 |
| `about-process-detail.spec.ts` | 5 |
| `map-near-me.spec.ts` | 5 |
| `city-search.spec.ts` | 4 |
| `home-filter-chips.spec.ts` | 4 |
| `map-legend.spec.ts` | 4 |
| `admin-pages-load.spec.ts` | 3 |
| `admin-status-labels.spec.ts` | 3 |
| `register-wizard-upgrade.spec.ts` | 3 |
| **13 specs** | **75** |

Plus `tests/test_manual_backend.py` — 15 `def test_`, 194 lines. The copy of THIS file on that branch carries
a 14-row per-section log with a `✅ green ×2` claim per section, and a "Runtime notes for the next checkpoint"
block worth reading before the first run.

**Two things that claim is NOT.** The `green ×2` lines are a previous session's report, not a run you have
observed — re-run before trusting any of them. And a viability probe on 01/09 found all 30 distinct
`getByTestId` values those specs use still resolve in `frontend/app` + `frontend/components` on `staging`
(the one apparent miss, `category-chip-1`, is built dynamically at `frontend/components/CategorySelector.jsx:249`
— a false positive of the probe, which was run with a fake-id control). **That says the locator layer survived
2,613 commits. It does not say the specs pass:** copy strings, routes and backend shapes can all move without
touching a testid.

**Do not carry `HANDOFF.md` across** — rule 31; `changelog-branch-guard` reds a code branch that touches it.
