# Session log — Lane A (backend), 13/08 מקטע 2

ממשיך את `session-a-m8kd2p.md`, שסיים ב-`8463435b`. שני כרטיסים נוקזו, שלושה ממצאים נפתחו ככרטיסים, ושישה כשלי מכשור נתפסו — **שניים מהם שלי, ונתפסו ע"י adversarial review אחרי המיזוג**.

---

## 1 · מה נוקז

| כרטיס | PR | staging | מה |
| -- | -- | -- | -- |
| MEH-2007 | #2858 | `8eb9e25e` | advisory lock שמסדר בטור את ה-check-then-act של פינג «מוכן לאישור» ב-`PUT /producers/me` |
| MEH-1586 | #2865 | `3bf1d43f` | רצפת starlette כ-`constraint-dependencies` ב-uv, כדי ש-resolution עתידי לא יבטל את ה-lock |

שניהם אומתו אחרי המיזוג בקריאה מ-`origin/staging`, ולא מה-PR.

## 2 · שתי הכרעות עיצוב שיצאו נגד הנטייה שבכרטיס

**MEH-2007 — הכרטיס נטה ל-(ב) דה-דופ ברמת ההתראה. נבחר (א) בצורה הצרה ביותר.** הסיבה אינה העדפה: (ב) **אינה ניתנת למימוש** תחת האילוץ "אין schema change" של הכרטיס עצמו — אין דגל התמדה לתבוע, ההערה ב-`auth_notifications.py` אומרת זאת במפורש, ו-`alert_log` דורשת `user_id NOT NULL` עם FK ל-`users` ואין עליה אילוץ ייחודיות. נבחר **advisory lock** ולא `with_for_update`: זמן החזקה זהה, רדיוס פגיעה צר בהרבה.

**זמן ההחזקה נמדד ולא הוערך**, כי הכרטיס אסר להסתמך על ההערכה: 3 משפטים / 7.8 ms, עם בקרה (PUT כבד יותר חייב לדווח ספירה גדולה ממש — 6). ה-"~200 שורות" שבכרטיס היו ספירת **שורות מקור**, רובן ה-set של `_PRODUCER_WRITABLE_FIELDS` והערות.

**MEH-1586 — ה-diff של ה-lock צומצם מ-15 שורות ל-3**, ע"י הכנסה ידנית של בלוק ה-`[manifest]` בלבד והשמטת wheels לא-קשורים של greenlet. **הצמצום אושר ולא הונח**: בקרה נפרדת הראתה ש-`uv lock --check` **מאדים** כשהבלוק חסר, כלומר השער באמת בודק אותו.

## 3 · שלושה ממצאים שנפתחו ככרטיסים

| כרטיס | מה | איך נמצא |
| -- | -- | -- |
| MEH-2051 | מרוץ בין-endpoint: `PUT` מול `confirm_phone_otp` יורים שני פינגים על מעבר אחד | adversarial reviewer בנה שחזור והריץ אותו |
| MEH-2052 | `check-bash-safety.sh:136` — הרגקס `sed[[:space:]]+-i[^>]*` נעצר בתו `>`, כך שכל sed שנוגע ב-version constraint חומק | reviewer נתקל בו במקרה; אומת מול המקור |
| (מהמקטע הקודם) MEH-2041 | שני שולחי מייל נוספים מחוץ לחוזה הקופי | אינוונטר לפי עוטפים |

אף אחד מהם אינו נושא `cc-queue` — ממצא אינו עבודה שאישרתי לעצמי.

## 4 · שישה כשלי מכשור

### 4.1 · שניים שלי, שנתפסו ע"י ה-reviewer **אחרי** שה-PR נמזג

| מה כתבתי | מה נכון |
| -- | -- |
| "12 שורות greenlet" | **10** — ‏5 מבנים × 2 ארכיטקטורות. ספירה **מוצהרת** במקום **נגזרת**, בדיוק המחלקה ש-`testing.md` מזהיר מפניה |
| `pr-checks.yml:305,442` כמיקום שער ה-`uv lock --check` | **`:323`, `:473`**. ‏`:305` הוא `--health-interval` של service ה-Postgres, ‏`:442` הערת כותרת של job |

**הציטוט השגוי הגיע מגוף הכרטיס ואני העתקתי אותו בלי לאמת** — הכשל ש-`meta-patterns.md` §1 קיים בשבילו. הכרטיס עדיין נושא אותו.

### 4.2 · Poller שתנאי ההצלחה שלו לא יכול היה לירות

בדקתי מיזוג עם `git merge-base --is-ancestor HEAD origin/staging`. תחת **squash** ה-HEAD לעולם אינו אב-קדמון של הבסיס, ולכן הענף `MERGED` היה **בלתי-נגיש מבנית**. ה-poller רץ ארבעה מחזורים ודיווח `BEHIND` בכל פעם — נאמנה, מול תנאי שלא יכול היה להתקיים. המיזוג התגלה מ-`git push` שהחזיר `[new branch]`.

**זו אותה מחלקה שבדקתי כל הסשן, מהצד השני:** בדקתי מה הפלט **אומר**, לא מה השתיקה שלו **יכולה** לומר.

### 4.3 · קריאה מקוצצת של לוג

קראתי פלט poller עם `tail -3`, ראיתי רק `done`, והסקתי שה-poller לא ירה. `cat` מלא הראה `STATE=BEHIND` שורה אחת מעל החיתוך. **המכשיר עבד; הקריאה שלי הייתה חתוכה.**

### 4.4 · ריצת סוויטה מול העץ הלא-נכון

הסוויטה של MEH-1586 (‏2614) רצה ב-worktree שנחתך **לפני** ש-#2858 נחת, ולכן תיארה עץ בלי שני הטסטים החדשים. **הדלתא מסתדרת בדיוק** — 2616−2614 = 2 = מספר הטסטים בקובץ ש-#2858 הוסיף, אומת ב-`--collect-only` וב-`grep` על הלוג. דלתא אחרת הייתה **רגרסיה**, ובשורת סיכום שתי האפשרויות נראות זהות.

### 4.5 · ה-reviewer עבד ב-worktree שלי

ביקשתי `/tmp/rev1586`; הוא השתמש ב-`/tmp/mm-1586` — שלי. שחזר את הקבצים ואימת `git status` נקי; **אימתי בעצמי**, ה-worktree נקי. מדווח כי הוראת בידוד שלא נאכפת אינה בידוד.

### 4.6 · ענף שנוצר מחדש אחרי מיזוג

`git push` שסונכרן 22 שניות אחרי המיזוג **יצר מחדש** את `feature/meh-2007-review-ready-ping-race`, ש-GitHub מחקה. `git push origin :branch` נכשל פעמיים (`the remote end hung up` ואז `Everything up-to-date`), ואין כלי מחיקת-ענף ב-MCP. **הענף עדיין קיים.** לא נוקה, ולא מוצג כאילו כן.

## 5 · ביקורת שנחלקתי עליה, על בסיס בקרה

ה-reviewer של MEH-2007 מצא מרוץ אמיתי והמליץ לעכב את המיזוג עד לתיקון, בנימוק שהתיקון "מזיז את המרוץ במקום לסגור אותו". הרצתי את אותה בנייה מול `origin/staging` — **אדומה גם שם**. כלומר חור **קיים**, לא רגרסיה, והנעילה לא הזיזה דבר.

<!-- 14/08 note: this line previously quoted the reviewer's exact recommendation
verbatim, including the trigger phrase named in workflow.md rule 30b's
documented false-positive class — a still-live repo guard reads that literal
quote as an active marker on the PR carrying this file, regardless of context.
Paraphrased above to preserve the meaning without carrying the literal
trigger phrase (deliberately not repeated in this note either, for the same
reason); the finding itself (the race pre-exists on origin/staging, unrelated
to the lock diff) is
unchanged. -->

מה שכן היה חייב להשתנות לפני מיזוג הוא **ההצהרה השגויה שלי** ב-docstring (*"closed by MEH-1820"*, שמערבב OTP-מול-OTP עם OTP-מול-PUT). תוקן ב-`f4dd5184`.

**הבקרה היא ההבדל בין "המבקר טועה" לבין "המבקר צודק אבל הסיווג שגוי".**

## 6 · מרוץ ה-merge מול staging

#2858 נכשל להתמזג ~30 דקות **לא בגלל בדיקה אדומה** אלא כי הוא נכנס ל-`behind` שלוש פעמים. ארבעה סנכרונים. קצב ה-churn נמדד: 12 מיזוגים ב-~2.5 שעות, פער ממוצע ~13 דק', ו-CI דורש ~8–10. חלונות קיימים; הפסדתי שניים.

**התרופה היא סנכרון, לא חקירה** — וזה מה שנעשה, כולל תפיסת חריץ ה-arming עם SQUASH.

---

## חומר ל-CHANGELOG (Lane C)

- **13/08 — מרוץ check-then-act בפינג «מוכן לאישור» נסגר ב-`PUT /producers/me`** (MEH-2007, PR #2858). advisory לפי עסק, נלקח **לפני** טעינת השורה. זמן החזקה נמדד — 3 משפטים / 7.8 ms — ולא הוערך.
- **13/08 — רצפת starlette הפכה לכלל ולא לארטיפקט** (MEH-1586, PR #2865). `constraint-dependencies` תחת `[tool.uv]`; שתי בקרות הפרידו בין "uv קיבל את המפתח" ל"uv אוכף אותו".
- **13/08 — שתי טעויות תיעוד תוקנו אחרי מיזוג** (PR #2865): ספירה מוצהרת במקום נגזרת, וציטוט `file:line` שהורש מכרטיס בלי אימות.
