# Pre-launch blocker audit (MEH-2085)

> **swept 122 of 175, chunk 3 of 3** — a resuming session reads this line and continues from it; it does not restart.

**Method.** Source of truth is Linear LIVE via `get_issue`, one card at a time.
`list_issues` is used ONLY as the index (IDs, titles, labels, state, priority):
its `description` is truncated at ~500 chars with a literal
`(truncated, use get_issue for full description)` marker, and that truncation is
**head-biased** — it deletes exactly the bottom-of-description rulings that decide
the verdict. Bulk-fetched descriptions are not admissible here.

**Criterion, applied in order.**
1. **BLOCKS** — a real user hitting the live site would be harmed, blocked or
   misled without it. Legal/regulatory exposure counts. Data loss counts.
   Security counts. "Would look unpolished" does NOT count.
2. **DEFER** — real work, no user harm at launch.
3. **MOOT** — the premise depends on data, history or scale that does not exist.

**Where the DEFER/MOOT line was drawn, stated so it can be argued with.** The
criterion is applied *in order*, so DEFER is tested before MOOT. A card that is
merely **trigger-gated** on scale — "revisit at 15+ businesses", "audit 30 days
after launch", "check when there is session-recording data" — still describes
real work with a real downstream deliverable, so it lands in DEFER even though
the scale it names does not exist yet. MOOT is reserved for a premise that is
**void or refuted**: the card's suspect was measured and is not there, or the
entity it acts on does not exist at all. Drawn the other way, MOOT would swallow
every post-launch card and stop meaning anything.

**Counts so far:** BLOCKS 11 · BLOCKS-COND 2 · DEFER 104 · MOOT 3 · FLAGGED 2

---

## 1 · BLOCKS

| ID | Title | Evidence |
|---|---|---|
| **MEH-2080** | No minimum-age check at registration | No age gate on either registration path — measured zero age/DOB fields in schemas.py and both clients. Minors can register and be contracted with; Amendment 13 + contractual-capacity exposure. CLOSE CALL: blocked on a lawyer ruling (MEH-1184), not on code — but 'blocked on a decision' is not 'not blocking'. |
| **MEH-1981** | ציות לתיקון 13 לחוק הגנת הפרטיות | Amendment 13 in force since 08/2025; site is live and collecting personal data (phone/email/address at producer registration, consumer accounts). Exposure is up to 10,000 NIS per claim with no proof of damage, for deficient collection notices or unmet access/correction rights. Squarely the criterion's 'legal/regulatory exposure counts'. |
| **MEH-1925** | [LIVE] Cloudinary 401 בפרודקשן | LIVE production incident, measured: 502 OPTIMIZED_EXTERNAL_IMAGE_REQUEST_UNAUTHORIZED on hero (home/login/register/events), About, producer hero, product images, and OG previews via lib/seo.js:110. Newest ruling sits at the TOP (11/08): cause is 439% Free-plan quota overage, not ToS disablement. A real user on the live site sees broken images today. |
| **MEH-2083** | 🚨 כל העסקים המאושרים בפרודקשן הם seed fixtures — הכרעה מה מוצג ביום הה | MEH-1992 (Done, PR #2799) concluded all 5 approved production rows are seed fixtures; public catalog rendered 4 on 14/08 with phone 050-1234567 repeated. A real owner Sapir phones will find the businesses she saw are fake — the site actively misleads, and it contradicts the three LOCKs the directory rests on (manual approval / licensed businesses only). Blocks the first outreach (MEH-409). |
| **MEH-1905** | 🩺 שני מוניטורים שקטים: Railway בודקת /health (alias שמקודד ok) בזמן ש- | CLOSE CALL — chose BLOCKS per the torn rule. Sapir's 14/08 measurements at the BOTTOM shrink this to one item and disprove the Sentry half (prod is quiet because it has no errors; DSN set in both envs). What remains: railway.json:8 healthchecks /health, an alias that hardcodes status ok, while /health/readiness returns 503 db_init_failed in BOTH envs. The deploy gate for production cannot fail. No user harm TODAY (all endpoints 200) — but the launch release is exactly when an undetected bad boot costs most. Ordering constraint: fix seed() (MEH-2081) BEFORE moving the probe, or Railway refuses deploys. |
| **MEH-409** | 🤝 First 10 producers from personal network — pre-launch supply seeding | The documented supply-side launch gate — the card and MEH-125 both name it as the gate, and MEH-2083 defers to it ('this card decides what is shown until MEH-409 happens'). STATED PLAINLY: this is a business/ops precondition (not-cc, a manual Sapir checklist of 10 personal-network producers), NOT a software defect. It is in BLOCKS because launching with zero real businesses is the harm, not because any code is wrong. |
| **MEH-1984** | ⬆️ next 16.2.12 → 16.3.0 — סוגר 3 CVE high שנעולים מאחורי pin של Next  | Three high CVEs live in the PRODUCTION dependency tree (npm audit --omit=dev = 3 high), pinned behind next@16.2.12's exact postcss 8.4.31 and sharp ^0.34.5, so no transitive bump can reach them. Security counts. The load-bearing half is sharp/libvips (GHSA-f88m-g3jw-g9cj): sharp processes every uploaded image at runtime and business owners upload images — the postcss path-traversal is build-time and weaker. Fix is one minor bump that satisfies the declared ^16.2.12. |
| **MEH-1959** | 🛡️ Security headers baseline — CSP ב-Report-Only + HSTS + nosniff (בלי | CLOSE — chose BLOCKS per the torn rule, with the scope narrowed honestly. The blocking half is HSTS + X-Content-Type-Options on a site that takes real logins at launch; their absence is an exploitable gap (SSL-strip, MIME confusion), and security counts. The CSP half is explicitly Report-Only and therefore protects nothing by itself — it collects violations. Do NOT read this row as 'ship enforcing CSP'; the card forbids that and it would break Cloudinary/tiles/fonts silently. |
| **MEH-1838** | 📝 ההרשמה לא לוכדת ציר משלוח כלל — כל עסק נרשם באותה צורה (RegisterProd | CLOSE. Reading both ends mattered here and reversed the answer: the 09/08 groom note near the top says the endpoint already accepts the fields; the 14/08 PARKED note at the BOTTOM corrects it with file:line — ProducerRegister (schemas.py:582-793) has none of has_physical_location/offers_delivery/delivery_nationwide/delivery_cities; they exist only on ProducerAdminCreate and ProducerUpdate. So every self-registered business arrives with a silent default and no delivery shape, and a consumer filtering for משלוחים cannot find her — a discovery failure on the product's core function (MEH-1822: 52 of 96 shape combinations unserved). Softened by concierge onboarding via the admin panel, where the fields DO exist; the public form is still live. Needs a backend chunk first. |
| **MEH-125** | MEH-125 — Pre-Launch Checklist: Redesign | The pre-launch checklist itself, and it is BLOCKS for the same reason MEH-409 is: a process precondition rather than a software defect. Two concrete things carry it. (1) MEASURED, not quoted from the card: launch-cohort.js:24 still holds the placeholder `LAUNCH_START = 2026-05-15`, three months in the PAST, so the +30d window is already closed and every user registering at launch is tagged `launch_cohort: null` — the Sentry Replay filter that MEH-434 exists to provide returns nothing on day one. It is a five-minute PR that must happen ON go-live day. (2) It is the ONLY place carrying real-device verification — iPhone Safari, Android Chrome, end-to-end registration, three persona smoke tests — and per rule 23's carve-out (e) there is NO WebKit engine anywhere in this pipeline, so no amount of green Chromium E2E substitutes for it. Launching having never opened the site on a real iPhone is an unbounded risk. Everything else on the checklist is genuinely deferrable; these two are not. |
| **MEH-1702** | 🌐 פער EN — 31 מפתחות חסרים (כולל aria-label של near-me) + 4 מחרוזות אנ | CLOSE, and I chose BLOCKS. MEASURED THIS SESSION, not quoted: flattening both message files gives he 3,886 keys vs en 3,872, with **30 keys missing from en.json** — and the split matters. Ten of them are on the PRODUCER REGISTRATION path (`auth.register.producer.*` — tagline label and placeholder, licence explainer, licence-pending opt-in label and hint, story card title and body, account reassurance, licence validation error, address privacy hint), i.e. an English-speaking business owner meets Hebrew mid-form on the site's core conversion flow. One more is `map.near_me_pill.aria`, confirmed absent — an accessible NAME, so an EN user gets an unnamed control, which is WCAG AA and therefore Israeli accessibility regulation (ת"י 5568 / תקנה 35 התשע"ג-2013), not polish. And this is publicly reachable TODAY: MEH-1698 restored the desktop language switcher, verified live at Header.jsx:462 (`<LanguageToggle variant="default" />`), so a visitor can reach the broken locale in one click. Cheap to fix — PR 1 is one JSON file and explicitly autonomous. Do NOT touch the 54 deliberately-identical keys, and note the separate 4 Hebrew-side strings need Sapir's copy approval (PR 2, draft). |

### Conditional — blocks only if a stated precondition holds

| ID | Title | Evidence |
|---|---|---|
| **MEH-2079** | Retention purge job — producer_page_views / alert_log / producer_whats | Zero scheduled-deletion sites on producer_page_views / alert_log / producer_whatsapp_clicks; viewer_ip_hash is pseudonymous (salted SHA256), not anonymous, and grows unbounded. BLOCKS **only if** the published privacy policy names retention periods — then the code contradicts a published promise. CONDITION UNVERIFIED: must read the live policy text. Volume is NOT the argument. |
| **MEH-1835** | 🚢 uvicorn ‏--workers 2 ב-Dockerfile — ‏concurrency ל-Railway [HIGH-RIS | 🔴 READ THIS ONE FIRST — it is the sharpest live risk the sweep found that CC cannot verify. The card is FROZEN, but it records an action that was ACTUALLY TAKEN: on 02/08 Sapir set `WEB_CONCURRENCY=2` on Railway following an instruction the card then retracted as WRONG, and it demands "הסרה מיידית (או =1)". The reason is measured from startup.py:105-143 — `_check_single_replica()` is LOG-ONLY, it prints a loud boot error and boots anyway, so nothing prevents two workers. With two workers, APScheduler runs twice (DUPLICATE ONBOARDING EMAILS to real business owners, duplicate watchdog), slowapi rate limits double, and the analytics/trending/JWKS caches split. BLOCKS **only if** the variable is still set to >1 on Railway today — CANNOT BE VERIFIED FROM THE SANDBOX (no Railway access, egress blocked). Two things for Sapir: (1) check the Railway env var now, remove or set to 1; (2) check whether duplicate onboarding emails went out between the 02/08 deploy and the removal — that consequence may already have landed on real people. The card's own lesson is worth keeping: "the guard reads X" is not "the guard enforces via X". |

---

## 2 · DEFER

| ID | Title | Evidence |
|---|---|---|
| **MEH-1754** | 🚨 resolver ממפה כל כשל fetch ל-notFound() — תקלת backend זמנית מוצגת כ | Bottom canonical table (12/08) supersedes the top: the SEO-critical half ALREADY SHIPPED — items 1-4 merged as PR #2514, recipe route as #2832, item 6 deleted, item 7 closed no-change. Only item 5 (env fail-fast) remains, and Sapir verified NEXT_PUBLIC_API_URL is set in Vercel preview+production, so the localhost fallback is latent, not active. No user harm at launch. |
| **MEH-1523** | ⚙️ שער DO-NOT-MERGE — מעבר מסריקת טקסט ל-label [מנגנון, לא regex] | Read in full earlier this session. Merge-process gate (marker moves from prose-scanning to a GitHub label); staged patch awaiting Sapir's YAML + label. Zero consumer surface — no live-site user can be harmed, blocked or misled by which mechanism blocks a merge. Real work, post-launch. |
| **MEH-2084** | 🟢➡️🔴 שער Repo guards ירוק מהסיבה הלא נכונה — tier C לא רץ אף פעם; open | CI false-green: Repo guards passes while tier C (backend Pydantic vs backend/openapi.json) never executes, and openapi.json is stale on staging since #2945 made city required. INDEPENDENTLY CORROBORATED this session — I hit the same drift locally (the city anyOf collapse) while running run-all.sh. Real work, fifth instance of the false-green class, but no live-site user is harmed/blocked/misled by a CI tier that does not run. |
| **MEH-2062** | 🚨 מכסת Vercel היומית (api-deployments-free-per-day) נכשלת על כל PR — א | Vercel api-deployments-free-per-day fails on every PR, so UI changes get no preview URL. CORROBORATED this session — PR #2948 carried the identical rate-limit status. Developer-workflow friction and a rule-9 reporting problem; zero consumer surface. Blocked on Sapir reading the Vercel dashboard (the 'does Ignored count' question is not derivable from the repo). |
| **MEH-1189** | 🧹 דאטה /producers — קטגוריית חוות הגליל שגויה + audit ישויות בדיקה ב-p | SUBSUMED by MEH-2083, which is why it is not BLOCKS on its own. Line 2 (test entities in prod) was already answered by MEH-1992/PR #2799: all 5 approved rows ARE fixtures. Line 1 (חוות הגליל categorised as דגים instead of בשר) misleads only while that fixture stays public — and MEH-2083's option א removes it. Line 3 is a copy preference, already locked to 'בשר בקר בהזנת מרעה'. |
| **MEH-1456** | 🗂️ Category slug + is_system ב-DB + stale-key reconciliation — Phase B | SEE ITS OWN PARAGRAPH (card requires it). By the audit criterion this is DEFER: no live-site user is harmed today by categories being keyed on a Hebrew display name. Considered for BLOCKS and rejected — the foot-gun (renaming a category in admin creates a duplicate row and drops the pin to DEFAULT, the MEH-1268 pattern) requires an admin action to fire, it is not spontaneous user harm. NOTE the card's own approved 24/07 ruling says chunks 0-2b run BEFORE launch; that answers a different question (cost), not the harm question. |
| **MEH-1517** | 🔁 אימות שחזור גיבוי אוטומטי ב-CI — מחליף את ה-drill הידני של MEH-1442  | CLOSE, and considered for BLOCKS because the criterion lists data loss. Rejected: an unverified backup does not CAUSE data loss, it fails to mitigate one, and docs/BACKUPS.md keeps the manual drill as a working fallback. Bottom of card (14/08): Sapir APPROVED the read-only secret and CC published the exact spec (STAGING_DATABASE_URL_READONLY + minimal GRANT), so STOP(a) opens the moment she creates it. Cheap and now unblocked — recommend doing it, but it blocks no user. |
| **MEH-1706** | 🌱 חוזה כיסוי ל-seed הדמו — 13 משטחים ללא אף שורה + שער CI נגד drift [3 | Bottom (14/08): Sapir opened the WAIT, chunks B+C ran — CHANGELOG records them landing via PR #2931. Staging demo-seed coverage + a CI drift gate. Its own §2.4 states the gate is CI-local and does NOT protect staging data. Zero production-user surface. |
| **MEH-1962** | ⚡ Lighthouse baseline — 5 עמודים מרכזיים בנייד: מדידה ×3, תיקון quick  | Lighthouse baseline on 5 mobile routes + mechanical quick wins. Core Web Vitals are an SEO ranking factor, which is a growth concern, not user harm — and the criterion explicitly excludes 'would look unpolished'. No measurement exists yet, so there is not even a known regression to point at. |
| **MEH-2072** | 📜 רישיון בלי תאריך תפוגה — עסק נשאר ציבורי אחרי שהרישיון פג: license_e | CLOSE — considered for BLOCKS because the 'licensed businesses only' LOCK is a public promise and a stale licence misleads. Rejected on timing: a business approved at launch has a licence verified minutes earlier, so the misleading state CANNOT occur at launch; it appears months later. The card's own argument is cost (retroactive collection), not launch harm. RECOMMEND landing it before MEH-409's first real approvals so capture coincides with onboarding. |
| **MEH-2034** | 💔 ‏/favorites הוא משטח המועדפים היחיד שלא מנוי למטמון המשותף — ביטול מ | CLOSE. Measured defect, not inferred: FavoritesClient.jsx:95/138 holds favorites in local useState with a one-shot fetch and never calls subscribeFavorites, while CardHeart and FavoriteButton both do — so un-favouriting on /favorites leaves the row until refresh (44 polls over 20s, run 31620486228). Rejected for BLOCKS: the DELETE succeeds server-side, nothing public is misrepresented, the surface is a logged-in secondary page, and a re-click is idempotent. Sapir already ruled the contract on 12/08 and the e2e assertion is staged to flip. |
| **MEH-1907** | 🚦 האגרגטור ממפה cancelled ל-FAIL — ביטול אינו כישלון, ומה שמחליט מיזוג | CI aggregator maps cancelled (an absence of measurement) to FAIL, the mirror of MEH-1582's skip-green; plus the finding that E2E gate is not a required context so 'wait for two green runs' is structurally unenforceable with auto-merge armed. Patch staged as PR #2867. Merge-machinery correctness — no live-site user surface. |
| **MEH-2077** | 🟢🔴 שערי אימות מדווחים ירוק בלי לאמת — contract probe עובר על ~160 מסלו | Third recurrence of the false-green class: the contract probe reported success while ~160 routes returned 302 to Vercel SSO. Chunk 1 (harden the probe to assert the status it saw) already landed — PRs #2929/#2930. What remains is chunk 2, the Vercel Protection Bypass secret, which is Sapir-only Vercel settings. A verification gate, not a consumer surface. |
| **MEH-1854** | 🏁 MEH-291 Phase 4 באמת — readers→enum, backfill+desync count, drop שתי | In flight with a Sapir-set schedule at the BOTTOM (14/08): chunk 1 = PR #2918, gated on self-QA screenshots, soaking until Mon 17/8 so a live Friday exercises the at-risk pill; chunks 2-3 are RED (Alembic column drops) and explicitly must not open before then, with a desync count and a fresh backup as preconditions. The residual user-visible edge (the Friday pill reading is_available_today unconditionally) is an availability-badge nuance, not blocking harm. NOTE: chunks 2-3 dropping legacy columns should NOT run pre-launch — their own gates say so. |
| **MEH-2046** | 🗺️ /map fulfillment IA — צ'יפי משלוח+איסוף מקודמים, תגי כרטיס, העברת ש | Large /map fulfillment IA chain, and mostly ALREADY LANDED — PRs #2855, #2880, #2894, #2903, #2904 and #2919 are attached. Discovery/IA improvement: promote delivery+pickup to a fixed chip row, fulfillment tags on cards, move the layer control. It does close the MEH-1836 divergence (a nationwide business passes the delivery filter but renders no badge), which is a real discovery defect — but that is being fixed inside this chain, not blocked on it. |
| **MEH-1896** | 🕳️ .loose() הוא top-level בלבד — אובייקטים מקוננים עדיין מסולקים, ושער | Bottom (14/08) closed the design fork: option ג only — extend the parity gate, do not hand-add .loose() to nested sites, because MEH-1748 decided to adopt codegen and a generated schema reflects nested objects automatically. Critically, the ONE live bug the audit found (delivery_areas[].delivery_fee) was already fixed under MEH-1942, Done, PR #2693 with a fail-to-pass test. What remains (locations[].opening_hours/.phone, categories[].producer_count) is latent or wholly inert — no consumer reads them. |
| **MEH-2052** | 🔓 הוק ה-bash-safety: הרגקס של sed -i נעצר בתו > הראשון — כל sed שנוגע  | Real coverage gap in CC's own guardrail: check-bash-safety.sh:136 uses [^>]* which stops at the first '>', so any sed -i editing a version constraint (>=, <=) escapes the deny — proven WITH a control (the same sed without '>' IS blocked, which is what rules out 'the file simply is not covered'). Protects the repo from the agent, not users from harm: zero consumer surface. Sapir-only either way — .claude/hooks/** is hard-deny in both directions (rule 32 corollary). |
| **MEH-2070** | 📱 לאדמינית אין נתיב נייד ל-/admin — הגאפ ש-MEH-1701 לא כיסה: הכרעה A ( | The admin has no mobile entry point to /admin — nav-registry declares admin as desktop-only with an in-code comment saying it is a known gap, not a design decision. Affects the ADMIN's operating convenience, not a consumer: desktop access works, and manual approval (the LOCK) is still performable. Needs a Sapir A/B decision before any code. |
| **MEH-1908** | 🔤 `show_all` — מחרוזת אחת משרתת שלושה צרכנים ושוגה בשניהם: ריבוי קשיח  | Live consumer-visible copy defect on producer pages: show_all has no ICU plural so count=1 renders 'הצג עוד 1 ערים', and the same string labels PICKUP POINTS as 'ערים' — wrong at every count, not just at 1. Reachable at 16 cities / 4 pickup points / 7 areas. Kept out of BLOCKS: it is wording quality, the criterion excludes 'looks unpolished', nothing functional breaks, and it is gated on Sapir's verbatim copy approval (rule 22) which is already drafted in the 09/08 ruling. |
| **MEH-2073** | 🔔 עסק מאושר עורך שדות רגישים בלי סיגנל — ping אדמין notification-only  | Notification-only admin ping when an APPROVED business edits city/phone/dietary-scope; PR #2928 attached. The underlying gap is real — MEH-1508 cross-checks dietary declarations AT approval, and the owner can change vegan_scope the next day with no signal. Kept out of BLOCKS because the label contract already presents diet flags to consumers as self-declared (labels.md), so the consumer is not told these were verified, and this card explicitly does not block the edit — it only signals. |
| **MEH-2076** | 🛡️ Review conflict-of-interest guard — חסימת ביקורת על בית עסק מאותה ק | Explicitly post-launch by Sapir's own 14/08 ruling recorded in the card. Closes the last conflict-of-interest hole in reviews (a business owner reviewing a same-category competitor). Real integrity work; the existing owner-guard, WhatsApp-click gate and Haiku moderation are already in place, so the surface is not unguarded at launch. |
| **MEH-1615** | 🔗 branch-name auto-link מנוגד ל-branch-name gate ב-PRs docs-only — MEH | Folded into MEH-1949 by the 09/08 orchestrator ruling — one guard fix (extend check-linear-mentions.sh to branch names), not three separate ones, and the root fix (disabling branch-name auto-link at Linear workspace level) is a Sapir integration setting. Ticket-hygiene tooling: it corrupts Linear statuses, never the live site. |
| **MEH-1736** | ⚔️ branch-name gate × rule 29 — סתירה מבנית: כל PR המשך לטיקט Done מהפ | Structural contradiction between the branch-name gate (requires meh-NNNN in the slug) and rule 29 (forbids a Done ticket's identifier), with 9 measured instances in BOTH directions — including the dangerous one where an OPEN card with a live Alembic human gate was auto-closed to Done three times. Sapir already decided the shape on 02/08 (disable branch-name auto-link at Linear workspace level) and 09/08 folded it into MEH-1949. Corrupts Linear status, never the live site. |
| **MEH-1943** | 🌐 קופי משתמש כתוב ב-backend — מחרוזות עברית קשיחות ב-HTTPException מצב | Chunk A landed (PR #2833). Three live occurrences remain: delivery_validation.py:44,78 quotes UI labels inside backend strings, producer_me.py:653 leaks the AVAILABILITY_STATES enum into a Hebrew sentence, producer_recipes.py:86 emits English plus raw UUIDs straight to screen. Error-copy quality on owner-facing forms — confusing, but nothing is blocked and nothing material is misrepresented. The one instance that DID point at a renamed UI label was already fixed under MEH-1940. |
| **MEH-1514** | 🫥 VRT /about מצלם עמוד שקוף — scroll-reveal נשאר opacity:0 אך תופס גוב | Test-infra coverage gap: /about VRT shoots a 5732px page of which ~1000px is visible, because FadeInSection uses whileInView (IntersectionObserver) and parity.css only neutralises CSS animation — so sections sit at opacity:0 while holding full layout height. The spec measures HEIGHT, not content, and a regression inside editors-pick/verification/tips would pass silently. No user harm; the scroll-reveal works correctly for real users. DISCOVERED sub-item carried to the discovered section: the founder portrait renders as a magenta block in every baseline and CC could not determine why — unverified whether it also happens on the live site. |
| **MEH-1748** | 🏗️ Spike — לייצר את סכימות ה-Zod מ-OpenAPI במקום לתחזק ביד: לסגור מבני | Bottom (14/08) is Sapir's architectural ruling: adopt Zod-from-OpenAPI IN STAGES. Phase 1 — generate in parallel to frontend/lib/generated/ plus a drift gate, additive-only, zero change to the live parse path — is approved and landed (PRs #2923/#2925). Phase 2 (swapping call sites) is explicitly NOT approved and is flagged HIGH-RISK. Build architecture; no consumer surface. |
| **MEH-1868** | 🔧 ארבעה שערים ב-warn-only הם ארבעה לא-שערים — ratchet עם baseline קפוא | Genuine live false-green in CI, and the sharpest one in the sweep: the mypy gate NEVER RAN — 'Failed to spawn: mypy', 0-second step, swallowed by || true, job reports success (CI job 91623752935). 20 real type errors sit behind a gate that looks armed. chunk 0's code landed (PRs #2614/#2806/#2829) but the workflow diff was never applied — pr-checks.yml:524 still carries continue-on-error and :541 still || true, both verified 11/08. Blocked on Sapir because .github/workflows/** is CC-deny. Zero consumer surface, so DEFER — but it is the reason to distrust that gate's green. |
| **MEH-1765** | 🎚️ VRT לא יכול לתפוס שינוי קופי — maxDiffPixelRatio 0.02 = תקציב 25,92 | Decided at the top by the 09/08 orchestrator ruling, which REVERSED the card's own recommendation: do NOT lower maxDiffPixelRatio (stays 0.02), because CI noise is real; instead add toHaveText assertions on 3-5 key strings for copy-bearing surfaces — both mechanisms, not one replacing the other. The measured consequence stands and is already written into .claude/rules/testing.md: a green VRT is not evidence the copy is unchanged. Test-coverage shape, no user harm. |
| **MEH-1742** | 🧪 שערים חייבים לאשר שמדדו לפני שידווחו מה מדדו — skip שמגיע לאגרגטור ח | Generalises the rule that a gate must assert it MEASURED before reporting what it measured, and that a skip reaching an aggregator must carry a reason so 'filtered, expected' and 'did not run, unexpected' stop arriving as the same token. Patch staged as PR #2866; the working precedent already exists at e2e.yml:224-249 (E2E coverage floor, EXECUTED > 0). CI semantics — the damage it prevents is misdiagnosis, not user harm. |
| **MEH-1897** | 📉 47 שדות backend בלי מקבילה ב-Zod — סיווג וצמצום הבסיס ששער MEH-1891  | CAVEAT WORTH READING: the frozen half is bookkeeping, but the half that still runs is a LIVE BUG HUNT — classifying which of the 46 undeclared fields are actually CONSUMED by a component today, i.e. read by the UI and silently stripped at parse. The precedent for what that finds is MEH-1704: 13 of 14 badge fields stripped, 11 of 12 badges dead in the grid. So this card could yet surface a BLOCKS-class defect; it has not been run. Count corrected 47 to 46 on 14/08 (order_window was declared). Declaration half frozen because MEH-1748's codegen will emit 44 of 46 automatically. |
| **MEH-2041** | 📧 עוד שני שולחים פונים-למשתמשת מחוץ לחוזה הקופי — newsletter welcome ( | Two more user-facing email bodies sit outside the copy contract's _CORPUS: the newsletter welcome (consumer-facing, with a full HTML part) and the pending nudge (to a business owner). Notably the original MEH-1965 grep SHOULD have caught both — they call send_email( directly — so the gap was in what was admitted to the corpus, not in the search. Adding assertions may red on copy that is LOCKED verbatim by Sapir (pending_nudge.py:66-71); the card correctly says that outcome is a finding for her, not a licence to edit. |
| **MEH-2054** | 📝 Carrier — session log Lane A 13/08 מקטע 3 (docs-only, כלל 31) | SPENT CARRIER — no work remains. Exists only because Branch name gate requires meh-NNNN in every legal branch slug, so a docs-only session log needs a ticket to hang a branch on. Its PR (#2869) is merged. Should simply be closed; it is backlog noise, not a launch consideration. |
| **MEH-2061** | 📝 Carrier — session log update, Lane C 13/08 continuation (docs-only,  | SPENT CARRIER — no work remains. Docs-only session log for Lane C 13/08; PR #2883 merged. Same branch-name-gate mechanic as MEH-2054. |
| **MEH-2066** | 📝 Carrier — session log final sweep, Lane C 13/08 (docs-only, rule 31) | SPENT CARRIER — no work remains. Docs-only session log final sweep, Lane C 13/08; PR #2893 merged. |
| **MEH-2074** | 📝 Carrier — session log Lane A 14/08 (docs-only, כלל 31) | SPENT CARRIER — no work remains. Docs-only CHANGELOG+HANDOFF for Lane A 14/08; PR #2917 merged. Its own body notes MEH-2059 and MEH-2056 are blocked, but those are separate cards, not work carried here. |
| **MEH-2036** | 🚚 פילטר ימי משלוח multi-select (OR) — pills מרובים, המשך MEH-1645/1771 | Delivery-day filter goes multi-select (OR); PR #2840 attached. Discovery enhancement — the single-select version works today. The card's own context says the value grows with supply density (~8 businesses per city now), which is the argument for later, not now. |
| **MEH-2048** | 🧹 pickup_points — עצירת שני נתיבי הכתיבה (admin+import) שמייצרים בוליא | Two write paths (admin.py:345, producer_import.py:287) set the pickup_points boolean that no consumer surface reads once MEH-2046's rows-only predicate lands, which makes the admin checkbox a control that lies. NOT MOOT: the write paths exist regardless of data. Harm is bounded today — Sapir measured count=0 businesses in the boolean-without-rows state on 13/08 — so nothing is currently mis-displayed; the risk is any future import or admin save creating one. |
| **MEH-2064** | 🧹 [RED · אחרי soak] drop products.price_range הלגסי — ה-follow-up שהמי | RED column drop that migration e4790e538aa2 promised as a follow-up in May and nobody opened until 13/08. Real work independent of data: the frontend fallbacks in ProductsSection.jsx / ProducerSections.jsx must go before the drop (reader-then-contract, ADR-007). NOTE the one part I cannot settle — the entry-condition count (products with price_range NOT NULL AND price_min NULL) needs a DB query; if it is >0 a backfill decision is Sapir's, and free-text like ₪45/ק"ג cannot be parsed automatically per the migration's own note. |
| **MEH-2043** | 🔤 StoryCardCanvas מוריד שני .woff2 מ-fonts.gstatic.com בזמן ריצה — זו  | StoryCardCanvas.jsx:35-36 fetches two .woff2 from fonts.gstatic.com AT RUNTIME in the user's browser, with version-pinned URLs (v21/v14) that Google can stop serving with no warning — while byte-identical local copies have been in the repo since PR #2838. Degrades the exported story card to a default font; it does not block anything, and the export surface is owner-facing. PR #2862 attached. The trap worth keeping: the local file is latin-only, and the canvas draws a HEBREW business name, so a careless fix breaks silently. |
| **MEH-1767** | 🛡️ Canonical-component guard — שער lint נגד שדות כתובת/עיר שנכתבים ידנ | ESLint guard against hand-rolled address/city fields instead of AddressSearch/CitySearch — the class shipped twice (MEH-1455, MEH-1766), so a guard is justified. Warn-only ratchet, zero product code, staged for Sapir as PR #2863 and labelled not-cc. Prevents a future recurrence; fixes nothing currently broken. |
| **MEH-903** | 🧹 Decommission `delivery_cities` dual-write column → migrate reads+wri | Chunk A merged (#1449); Chunk B closed with Q1=0 on BOTH staging and prod (Sapir, 03/07). All that remains is Chunk C — dropping the `delivery_cities` column (RED, Expand-Contract) — which the card itself explicitly parks until after launch. The dead column is read by no user path and written by none, so leaving it costs a consumer nothing. |
| **MEH-1625** | 📋 הרחבת מטריצת ה-personas ב-MANUAL_TESTING — P5 (מקסימלי + תווי קצה +  | The documentation half already shipped (PRs #2816, #2828 — the persona was renamed P5 to P6 because P5 was taken). What remains is the physical execution of runs P1-P5, blocked on the staging backend returning non-2xx. That is QA debt on a test environment, not a defect a production user meets. |
| **MEH-1428** | ✨ "בקשו ביקורת" — קישור ביקורת חתום שבעלת העסק שולחת בעצמה (מודל Thumb | New feature (signed «בקשו ביקורת» review-request link, Thumbtack model) with no existing defect. Worth flagging: it is the technical dependency of MEH-409 item ה' (3-5 seed reviews per business before public launch), so if Sapir chooses to collect those through the product rather than by hand, the timing becomes pre-launch. Still DEFER — seed reviews can be collected manually, so this is not a hard dependency. |
| **MEH-1272** | 🤖 [Post-launch] V2 אימות רישיון אוטומטי — הצלבה מול dataset פתוח של מש | Explicitly labelled post-launch and decision-first, with a stated trigger of 15+ active businesses (or a felt manual-verification load). No execution prompt was written on purpose. Automates what MEH-1271 does by hand; the manual baseline stays the guarantee, and its own DoD forbids auto-rejection (the manual-approval LOCK survives). Nothing here can harm a launch-day user. |
| **MEH-1399** | 🗂️ Admin review checklist — Phase 2 (DB-editable + per-producer audit  | Phase 2 of the admin review checklist — moving the 7 items from static config into two new tables plus a per-approval audit trail. The 09/08 groom note at the bottom records the blocker clearing: Phase 1 shipped in PR #1998, so this is ready but unprioritised. Admin-internal surface with a working Phase 1 underneath; a consumer sees none of it. RED migration, so it needs Sapir's hands regardless. |
| **MEH-1430** | 🎨 LocationModal Refresh — C hybrid: bottom-sheet בנייד + dialog ב-desk | Approved mockup (21/07) restyling LocationModal into a mobile bottom-sheet + desktop dialog. The card's own words: "שכבה ויזואלית בלבד, אפס שינויי התנהגות" — every behavioural contract (useFocusReturn, Esc, MEH-1192 inline geoError, z-[9000], onSelectCity) is to be preserved verbatim. This is squarely the criterion's excluded case: a surface that "would look unpolished" is not a blocker. |
| **MEH-1529** | 🧪 schemathesis 4.21→4.24 — pytest נהרג בטיימאאוט; post-launch (מחליף א | Card is explicitly post-launch in its own title and dependency block ("⏸️ post-launch. לא להתחיל לפני"). schemathesis is a test-fuzzing tool with zero product code; the lock sits on a working 4.21.1 and the bump is a floor raise, not a CVE. Note for Sapir, not a blocker: without MEH-1518 Phase E's declarative dependabot ignore, the bump reopens itself weekly. |
| **MEH-2049** | 🔗 /map — סנכרון פילטרים↔URL (אין deep-links, רענון מאפס הכל) [post-lau | /map filters have no URL representation at all (verified by grep in MEH-2046 Phase 0: zero searchParams/router.push under app/[locale]/map/**), so a refresh loses them and no filtered map can be shared. Real gap, but it is friction and a missed SEO/deep-link opportunity, not harm — nothing is misrepresented and no task is blocked. Also hard-blocked by MEH-2046, which fixes the final filter set the URL must encode. |
| **MEH-263** | 📋 LocationModal z-index חוסם GPS button — לבדוק אחרי launch: UX אמיתי  | Observation card, not a task — its own text says "זהו תיעוד בלבד" and gates on 50+ businesses plus session-recording data that does not exist yet. The measured fact is real (LocationModal fixed inset-0 z-[9000] covers the GPS button at z-[1000]) but the card deliberately declines to rule whether that is the intended flow or a defect, and answering it needs production traffic. NOTE ON THE LINE DRAWN: trigger-gated research cards like this are DEFER, not MOOT — the criterion is applied in order, and there IS a downstream deliverable once the trigger fires. MOOT is reserved for a premise that is void or refuted. |
| **MEH-569** | 📊 Adversarial-review effectiveness audit — 30-day post-launch (TP/FP/F | A 30-day post-launch effectiveness audit of /adversarial-review, by definition unrunnable before launch + 30 days — a schedule, not a missing premise. The 09/08 groom note at the top usefully widens what it must measure: MEH-1844 found the CI reviewer INTERMITTENT, so PRs that got a silent no-op are "no data", not false negatives, and need their own field rather than being absorbed into TP/FP/FN. (That intermittency was itself later reported resolved in workflow.md, 13/08 — worth re-deriving when the audit runs rather than inheriting either claim.) |
| **MEH-725** | feat: tokenize 5 #e8e0d0 icon-fill literals (skeleton shimmer + empty- | Five #e8e0d0 literals used as icon fills (skeleton shimmer + empty-star tint), deferred pending a brand ruling on whether a disabled/muted-surface token should exist. The card states the key fact itself: there is no visual bug — the sites render at #e8e0d0 today and would render identically after any of the three options. Pure internal consistency; zero rendered difference for a user. |
| **MEH-1007** | 📊 analytics data enhancements — שעות שיא · פילוח שכונתי · דלתא מספרית  | Placeholder for three analytics enrichments (hourly peaks, neighbourhood breakdown, numeric per-KPI delta) that the dashboard design flagged as NEEDS-BACKEND. Its own text says none of them blocks MEH-964, no prompt is written, and the trigger is post-launch. Owner-facing analytics depth, not a consumer surface. |
| **MEH-1055** | 🔴 3 skills מסומנים approved בלי עוגן hash — חור חי בשרשרת האמון של MEH | CLOSE, and the one row where I depart from Sapir's own label — she moved this to PRE-LAUNCH on 07/08, and the card is right that it is a live hole in the MEH-420 trust chain: skills marked `approved` but absent from `skills-lock.json` have NO hash anchor, so their content can change post-audit with CI staying green. Rejected for BLOCKS on the criterion as written: the exposure is CC's own tooling supply chain, and reaching it already requires repo write access, which buys an attacker the application source directly. No live-site visitor is harmed by it. STRONGLY RECOMMEND doing it anyway (four commands, Sapir's hands only — CC writing its own trust anchor voids the anchor, rule 32). READ THE 12/08 UPDATE FIRST: it is FOUR skills not three (`backlog-groom` was added 09/08 with the same defect, proving the hole is in the process), and locking two of them now would ratify unaudited content — `mehamakor-dod` has four commits after its audit date, and `grilling`'s audit date PREDATES its only commit by 14 days. Lock `grill-me` and `backlog-groom`; re-audit the other two first. |
| **MEH-1154** | 🐛 hero-search double-mount — ה-flake חוזר; MEH-1078 סטה מה-AC שלו וה-d | Explicitly deferred post-launch by Sapir (12/07) with the reason recorded in the card, and Phase 0 confirms it: the duplicate exists for milliseconds during hydration and the ids are already unique (MEH-1078 did fix that), so there is no a11y impact and no user-visible effect. Test-infrastructure flake only; the cost is an occasional CI re-run. |
| **MEH-1166** | 📅 ניהול אירועים/חוויות לבעלת העסק — list/edit/cancel (create-only היום | CLOSE — there IS a harm path and it should be named: events are create-only in the owner UI, so a business owner cannot edit or cancel a published event, and a consumer can act on stale public information. Rejected for BLOCKS on three grounds: the owner PUT/DELETE endpoints already EXIST (MEH-1001), so the gap is UI-only; the admin panel gives Sapir a working remedy; and with ~10 personally-onboarded businesses at launch (MEH-409) a phone call closes it. Also carries two explicit governance decisions — deferred post-launch 12/07, frozen by the 09/08 orchestrator ruling — and the card is a stub with no UX/copy spec, so it is not buildable as written. |
| **MEH-1207** | 🧭 עמוד בית עסק — שורת ניווט-עוגן (sticky, data-gated) [חסום ע"י MEH-11 | Sticky anchor-nav ("jump to section") on the producer page, sourced from competitive research. The 09/08 groom note clears its stated blocker (MEH-1146 is Done, section order documented at docs/DESIGN.md:348), so it is ready but unstarted. Pure navigation convenience on a long page — nothing is wrong today, the content is all reachable by scrolling. |
| **MEH-1208** | ✨ עמוד בית עסק — סקציית "למי זה מתאים" (schema + dashboard + display)  | New "who is this for" section: a column on producers plus endpoint, dashboard editor, public display and i18n — four chunks with a RED migration, not a small change. Still blocked on a Sapir data-model ruling (free text vs structured bullets) with no prompt written by design. Adds a persuasion surface the page does not have; its absence misleads nobody. |
| **MEH-1214** | 🖼️ §10 "הכירו בית עסק" נעלם בשקט → דף הבית = 3 בלוקים טקסטואליים ברצף  | The §10 featured-producer block self-hides (HomeStaticBlocks.jsx:199 `if (!featured) return null`) because no producer carries is_recommended AND short_description, leaving three text blocks in a row on the homepage. The code is correct and the card says so — this is a content/data task with no CC prompt. Route B is hard-blocked by MEH-409; route A (flag the staging demo business) is open and worth doing for design QA and VRT, since the block has never been seen rendered with real data. Frozen by the 09/08 orchestrator ruling. Visual pacing is exactly the excluded "would look unpolished" case — and the card's own position is that empty space beats stock imagery. |
| **MEH-1231** | 📰 מדריכי תרחיש — שכבת מגזין ראשונה (Infatuation pattern): design + 2 מ | Scenario-guide magazine layer (Infatuation pattern). Design is done to full mockup with a locked direction; the code ticket was deliberately NOT created. Explicitly data-gated by Sapir (16/07) on 3+ real approved businesses in one category, with a standing ban on shipping the fictional mockup names to prod. New editorial surface, nothing broken without it; the mockup already earns its keep as a recruitment asset. |
| **MEH-1286** | ✨ Homepage — מודול "חדשים אצלנו" (בתי עסק שאושרו לאחרונה) — סיגנל רעננ | "New here" homepage module — the 4 most recently approved businesses, as a freshness signal against Israeli directories that visibly rotted. Its own render gate requires ≥3 businesses approved in the last 60 days, so with zero real approvals it would render nothing anyway. New surface, no defect. The 09/08 groom supersedes its WAIT gate, so it is unblocked whenever it is prioritised. |
| **MEH-1287** | 🍂 Homepage — מודול "עכשיו בעונה" (curation עונתית ידנית) [data-gated] | "In season now" homepage module, manually curated. Explicitly data-gated on 3+ real approved businesses and frozen by the 09/08 orchestrator ruling; the curation mechanism (tag/flag/list) is still an open Sapir decision, so there is nothing buildable. Worth noting for MEH-409: the 22/08 research SYNC makes this a recruitment hook for seasonal farmers ("your business will appear in 'in season now' at harvest"), which is a supply-side argument, not a launch-harm one. |
| **MEH-1303** | 💬 תשובות לשאלות מותאמות של בעלת העסק (custom Q&A pairs) — owner-seeded | Phase 2 of Quick Answers — letting an owner attach an ANSWER to her custom questions (today `custom_questions` is `list[str]`, every tap goes to WhatsApp). Triple-gated by its own text: post-launch, frozen 09/08, blocked on a Sapir data-model ruling (JSONB pairs vs table), and dependent on MEH-1302 being live in production with feedback first. The WhatsApp fallback means no question goes unanswered — it just costs a message. |
| **MEH-1337** | 📊 Ratings distribution summary בביקורות — trigger: כשחציון הביקורות לע | Star-distribution bars above the reviews section. The card is explicit that shipping it now would be worse than not: with ~8-12 reviews per business there is nothing to distribute and the feature looks empty. Trigger is a median above ~20 reviews per active business, which cannot be true at launch — MEH-409's plan is 3-5 SEED reviews per business. |
| **MEH-1414** | 🗺️ /map — שימור center+zoom בחזרה מעמוד בית עסק + לינק "חזרה למפה" (an | CLOSE, and the evidence is Sapir's own 21/07 mobile screenshot: pan the map, tap into a business, press back, and you land on the Israel-wide default camera (`[32.4, 34.95]` zoom 8) instead of where you were. Real pogo-sticking defect on the primary discovery surface, and High priority. Rejected for BLOCKS on density: the problem only bites once there are enough businesses that you must pan to see them, and at launch MEH-409 puts ~10 on the board — they fit in the default camera, so the "lost my spot" case barely arises. Recommend it early post-launch, before density makes it a daily irritation. Collision-watch: MEH-1412 touches the same files. |
| **MEH-1416** | 🧭 Dashboard IA Phase 2 — ניווט-לפי-אובייקט (טאב "תוכן", ביקורות נשארות | Dashboard IA Phase 2 — renaming the "tools" tab to "content" and applying the MEH-1408 hub-card pattern. Explicitly post-launch and explicitly shell-only: the card says the underlying feature management already exists (MEH-1405 Done, PR #2017), so this reorganises navigation the owner can already reach. A direction-documentation ticket with no Model+Effort block, i.e. not yet dispatchable. |
| **MEH-1495** | ⏰ Google Cloud free trial נגמר ~15/10/2026 — להפעיל חשבון מלא אחרת שור | Operational reminder with a real date: the Google Cloud free trial funding GOOGLE_PLACES_API_KEY runs out around 15/10/2026 (₪898 credit, 84 days left as of 23/07), and the endpoint fails QUIET — an API error returns 204 and the Google rating row vanishes from every producer page with no error anywhere. Worth Sapir's attention as a diary item, but the deadline is two months out, the failure degrades one optional row rather than breaking a page, and the card is a checklist for 01/10 rather than work. |
| **MEH-1501** | ⚙️ CI לא נטרג מ-PR שנפתח ע"י CC — GITHUB_TOKEN חוסם workflow runs (מחק | CI does not trigger on a PR CC opened, because GitHub suppresses workflow runs for events created by GITHUB_TOKEN. Developer-workflow only, zero consumer surface. Mostly resolved already: workflow_dispatch merged (#2101) and a reproducible workaround identified and proven twice (push via the CCR git relay, which needs a NEW sha — a re-push of an existing tip is a silent no-op). What remains is Sapir granting `actions: write` (urgency explicitly downgraded) plus one undone line in `.claude/rules/`. Worth reading for its three DISPROVEN theories — notably that committer identity decides, refuted directly on #2121 — which the card warns must not be codified. |
| **MEH-1522** | ⚛️ React 18→19 migration — post-launch (מחליף את dependabot #1506 שנסג | React 18→19. Sapir ruled it post-launch on 23/07 in the words the card records — "a migration, not a bump; two weeks before launch, no" — and the declarative `ignore` is already in dependabot.yml so it will not reopen itself. Nothing renders differently on 18; the whole case is toolchain currency. Phase 0 is read-only and unstarted, so the blast radius (blocking peer deps, Next↔React 19 compatibility) is still unmeasured. |
| **MEH-1621** | 🧹 עברית בהערות Python — חוב repo-wide (10 ידועות ב-models.py, השאר לא  | Hebrew prose in Python comments — old debt, 10 known lines in models.py and the rest never counted. Zero user impact by the card's own words, and a repo-wide sweep during heavy parallel merging costs more in conflicts than it returns. Read the 02/08 narrowing before touching it: QUOTING a rendered UI label inside an English comment is correct and stays (85 frontend files do it); only Hebrew prose as the comment BODY is in scope. The doc half (narrowing CLAUDE-REVIEW.md:117 so the reviewer stops flagging label quotes) is small, already has PR #2527 attached, and can be done ahead of the rest. |
| **MEH-1641** | 🔐 [post-launch · RED] Auth נראה ל-edge/SSR — מעבר JWT מ-localStorage ל | CLOSE on the security half. The JWT lives in localStorage and is attached only by an axios interceptor (lib/api.js:15-22), so middleware/SSR see every user as anonymous — the shared root of four documented symptoms — and localStorage is readable by any JS on the page, i.e. XSS-exposed where the standard is httpOnly+Secure+SameSite. Rejected for BLOCKS: exploiting it requires an XSS first, and this repo runs a strict CSP; the visible symptoms (MEH-1632's 404 for a pending owner, MEH-1638's 800ms blank) already have working workarounds. RED tier, post-launch by explicit decision, and the CORS/cross-origin question (Vercel↔Railway) is unresolved — not a change to start under launch pressure. |
| **MEH-1677** | 🧩 Coverage capture (Phase B1, pre-launch) — עיר על אירוע הקליק + cover | CLOSE, and it carries the strongest do-it-now argument of any DEFER row: capture the city on each "you don't deliver to me" CTA click. Sapir ruled it PRE-LAUNCH on 27/07 and the reason is irreversibility — without day-1 capture every click loses its city permanently, and MEH-1676's demand-by-city dashboard card arrives post-launch to find no history. Rejected for BLOCKS because no user is harmed, blocked or misled by it; the loss is ours, not theirs. Same shape as MEH-2072: cheap now, unrecoverable later. Its blocker cleared (MEH-1577 Done 27/07); re-derive the Alembic head before writing the revision, and note merge = apply, so Sapir's approval sits on the MERGE. |
| **MEH-1685** | 🔍 [post-launch, data-gated] Autocomplete polish — inverted highlight,  | Autocomplete polish to Baymard 9/9 — inverted highlighting, producer thumbnails, mobile focus mode. The engine (MEH-99) already works; this is finish. Explicitly post-launch and double-gated: staging→main release plus ≥10 approved businesses, because below that the suggestion panel is nearly empty. Note its own warning, which is rule 29b in miniature: do not use this ticket number in a branch slug before the gate opens — a branch name alone closed a ticket on 26/07. |
| **MEH-1694** | 🎨 ליטוש עריכתי לאזור הזהות והקשר — מוקאפ v3 מאושר: ירוק אחד, אפס קופסא | Editorial polish of the identity/contact area on the producer page, mockup v3 approved 28/07 (one green on the page, zero boxes, one trust row). Explicitly restyle-only: every behavioural contract is LOCKED byte-unchanged (pull-quote self-hiding, #reviews anchor, WhatsApp chips, trackContactClick, 44px targets). Two open Sapir decisions remain (Instagram placement; the Waze/Google half was closed on 07/08 — external navigation via official deep links, no internal "see on big map"). Pure visual refinement, which the criterion excludes. |
| **MEH-1753** | 🏷️ חוזה ה-labels לא מכסה את BADGE_CONFIG — התווית הדינמית של MEH-1711  | The label scope contract (MEH-1507) governs ATTRIBUTE_LABELS/CHIPS_CONFIG/TOGGLE_CHIPS but not BADGE_CONFIG, so the badge copy a consumer actually reads sits outside the guard — including MEH-1711's dynamic kashrut label. Phase 0 measured all 12 entries failing, so the debt is uniform and lands in one PR. Real integrity work on a surface where a mis-scoped label over-claims (the MEH-986/1259 family), but nothing is mis-scoped TODAY — this adds the guard that would catch a future one, and its own DoD requires ZERO rendered strings to change. The 09/08 ruling unblocked it: `editorial` joins as a fourth evidence value for `recommended`, `new` and `products` get `system`. Watch the copy trap it names: 7-8 overlapping keys must be imported or assertion-guarded, never hand-copied, or the guard recreates the drift class it exists to prevent. |
| **MEH-1768** | ✍️ [quick] שאלת ריבוי המיקומים בהרשמה — micro-copy קבוע מתחת לשאלה במק | One-line copy move in producer registration: show the multi-location helper ("אפשר להוסיף את כל הנקודות בלוח הבקרה אחרי ההרשמה") permanently under the question instead of only after the checkbox is ticked, so the question reads as information rather than a required field. Copy already approved by Sapir 29/07, LOW-RISK, end-to-end authority — the cheapest card in the sweep. Not a blocker: the field works, the explanation is merely one click away. |
| **MEH-1829** | 📍 מיון "מרחק" ב-/producers — sort=distance ללא פילטר רדיוס (post-launc | Third sort option ("distance") on /producers, closing the gap with /map. Explicitly post-launch and data-gated at ≥30 approved businesses, because below that a distance sort shows an apparently-empty catalogue to anyone outside the centre. Its EPIC dependency cleared (MEH-1388/1402 Done), so only the data gate remains. Worth preserving its core design ruling when it runs: a SORT must never HIDE — no radius filter, NULL-coords rows last, and a numeric assertion that X-Total-Count is identical to sort=newest. |
| **MEH-1839** | 🔧 stop-hook של חתימות מדווח כשל עם שתי סיבות שגויות — הוא לא מבחין בין | The signature stop-hook reports failure with TWO explicitly-stated reasons and both are false in this container: the commit DOES carry a `gpgsig` SSH signature, and the committer IS `Claude <noreply@anthropic.com>`. The `N` from `%G?` means git cannot VERIFY locally (`gpg.ssh.allowedSignersFile` unset, `user.signingkey` points at a 0-byte file), not that a signature is absent — so the prescribed `--amend --reset-author` rewrites correct history for nothing. Real, and a nice specimen of the "instrument you have not checked" class (the card notes it was the FOURTH confidently-wrong check in one session), but it lives at ~/.claude/, outside the repo, so it is Sapir's and not fixable by PR. Zero consumer surface. |
| **MEH-1850** | 🗑️ Contract: הסרת העמודה has_delivery — בוליאני שני ומת ל"משלח", אף קו | Contract-phase drop of `producers.has_delivery`, a second dead boolean for a concept `offers_delivery` already owns — no dashboard editor writes it and the public filter consults `delivery_areas.any()` rather than the column. Its blocker cleared (MEH-1849 Done 03/08). Internal schema hygiene: nothing a user sees changes, and the column is inert either way. Two traps to carry forward when it runs — the public `?has_delivery=` query PARAM keeps its name (only the column goes), and MEH-903's lesson applies in full: an empty column is not a dead column, so the writer+reader census comes BEFORE the drop, on production as well as staging. |
| **MEH-1865** | 📅 /events — הפרדת empty-dataset מ-filtered-to-zero בשני הטאבים (שער ה- | /events renders filter controls above zero content, and the fix separates two different zeros (empty dataset → no filters + editorial empty state + link to /producers; filtered-to-zero → keep filters + "נקו סינון"). Guaranteed visible at launch, since there will be few or no events — but an empty state ALREADY exists (EventsClient.jsx:488), so this refines presentation rather than fixing a hole. Sapir has ruled everything (03/08, second round), the chrome gate is cancelled and Footer.jsx is out of scope. Worth doing early; not harm. Its Phase 0 is a good STOP precedent: the original spec assumed one surface and would have deleted an entire tab. |
| **MEH-1889** | 🕎 [post-launch] שעות מיוחדות לחגים פר-עסק — special_hours overrides (ד | Per-business special hours for holidays (Google/Yelp pattern) — date-keyed overrides above the weekly axes. Explicitly post-launch, RED (Alembic) and design-first, with three open Phase 0 decisions and no closed prompt. A site-wide holiday mode already exists (MEH-247). Real seasonal value — Israeli holiday eves are peak, and wrong hours then means a wasted trip — but that is a feature gap, not a defect, and both consumption surfaces it would feed (MEH-1875 schedule block, MEH-1881 open-now filter) only landed on 03-04/08. |
| **MEH-1890** | 🧹 [post-launch] סיווג ~50 מופעי datetime.utcnow()/now() → datetime.now | Classify ~50 `datetime.utcnow()`/`now()` sites and modernise only the UTC-instant class. The card did the important check already: the main user-facing suspect (`kashrut_expires_at`, written admin_kashrut.py:87, compared producer_listing.py:309,316) is self-consistent on the UTC axis, so this is maintenance debt and NOT a bug — the genuinely wrong-semantics sites were MEH-1883, which is Done. Its blocker cleared. A blind sweep is the wrong move; the per-site table comes first, since a naive/aware comparison against DB-loaded values is where this breaks. |
| **MEH-1913** | Tailwind v3 → v4 — מיגרציה ידנית (מחליף את dependabot #2132 שנסגר) | Tailwind v3→v4 is an engine replacement, not a version bump — three files change shape (globals.css directives, JS config → CSS-first, a separate PostCSS package) and the caret pin will not do it by itself. RED tier, post-launch, touching every visual surface at once in a full-RTL project. The card carries the sharpest warning worth preserving: VRT is NOT a safety net here — maxDiffPixelRatio 0.02 is a ~25,920px budget on desktop, so an engine change can shift spacing across the whole page and stay green (MEH-1765). Every regenerated baseline must be opened and read. Side note it raises correctly: `tailwindcss` is missing from dependabot.yml's `ignore` list, so #2132 reopens weekly until MEH-1518 adds it. |
| **MEH-1944** | 🧹 שלושה advisories מ-adversarial review של עמודי הדיאטה — dedup fetch, | Three advisories from the adversarial review of PR #2665 (diet landing pages), all explicitly ruled non-blocking and parked here so they do not vanish with the PR: a double fetch per render (correctness unaffected — two identical requests, both with revalidate 60), a silent `.catch(() => 0)` on sibling chips (fail-open is correct and reasoned; only observability is missing), and a hard PER_PAGE = 24 with no "showing 24 of N" disclosure. The third is a spec question for Sapir, not a bug — the card is right to leave it alone. The two real fixes are one file and low effort. |
| **MEH-1980** | 📐 תקרת כיסוי טסטים + ratchet — סוף-סוף מספר: איפה הקוד חשוף, ושער שמונ | Coverage ceiling plus a ratchet. Thousands of tests (2,567 vitest, 2,431 pytest) and no coverage NUMBER, so nobody knows which areas are exposed — real, and the ratchet pattern is already house style (MEH-1868). Frontend half has a PR attached (#2813, freeze at 66.77%, block regression only). Test-infrastructure work: it changes no product behaviour and blocks no user. NOTE for rule 29b watchers: #2813 merged carrying `Refs MEH-1980` and did NOT close the card — one of the five measured `Refs` outcomes, and the reason that row exists in workflow.md. |
| **MEH-1985** | ⬆️ vite 8.0.8 → 8.2.1 (dev-only) — 2 CVE, אבל גורר rolldown rc→stable  | vite 8.0.8 → 8.2.1 closing two advisories, and the card's own measurement is the argument against rushing it: with vite in the update 54 packages move (+263 lines) vs 24 without, because it drags rolldown from release-candidate to stable — i.e. it swaps the bundler that runs vitest. Both CVEs are Windows-only and dev-server-only; `npm audit --omit=dev` does not report vite at all, so neither is reachable from CI or production. Trading the test toolchain for two unreachable advisories is not a launch-week move. |
| **MEH-1988** | 🕐 עקביות שעון-ישראל בצד לקוח — שלושת נתיבי התצוגה שנותרו + שער נגד מימ | The card says it itself and I agree: "this is consistency, not correctness". The real timezone bug — client and server disagreeing about what day it is, breaking the vacation_until picker — was closed by MEH-1983 (PR #2737). The three remaining sites have no server counterpart to disagree with: holidays.js is display-only, the seasonal placeholder is at worst a neighbouring season, and CalendarView may be CORRECT as browser-local (the card explicitly refuses to assume otherwise). The valuable half is the guard, and its framing is worth keeping: MEH-1983's lesson was not three inconsistent files but that OfferBadge.jsx held the right knowledge in a PRIVATE function's docstring while two other files did what that docstring forbade. |
| **MEH-2004** | 🧯 צבע הקטגוריה מוזרק גולמי גם ב-MiniMap.jsx וב-marker-glyph.js — אותה  | Two more surfaces inject the category colour raw into markup (MiniMap.jsx:102-117 into a style attribute, marker-glyph.js into SVG), siblings of the class PR #2771 closed for MapComponent.jsx. DORMANT TODAY and the card proves it: all 9 CATEGORY_STYLES values are hardcoded `#rrggbb`, and unlike #2771 there is no `onerror` JS context, so there is no escalation path — only an attribute-boundary break if a colour ever contained a double quote. It arms the moment colours become DB-driven. Right fix named correctly: validate once in `styleForProducer` rather than hardening each consumer, and note the card's warning that `escapeHtmlAttr` was measured INERT for the onerror context. |
| **MEH-2010** | 🖼️ מסלול ה-aspectRatio ב-optimizeCloudinary ללא כובע — c_fill,w_1200 מ | The `aspectRatio` path of optimizeCloudinary has no width cap, and `c_fill,w_1200` UPSCALES a source narrower than 1200 — measured against the Cloudinary Admin API at +68% bytes (46,363 → 77,706) for zero added detail. Real mechanism, and it touches the Cloudinary quota that MEH-1925 (BLOCKS) is bleeding on. Still DEFER, on the card's own honest volume note: exactly ONE asset in the account currently has the narrow-portrait shape, and this was NOT part of the 109-credit burn (that was full-source delivery, closed by MEH-2001). Worth closing BEFORE growth, since every owner uploading phone portraits adds to the shape. |
| **MEH-282** | ♻️ Migrate GSI integration to @react-oauth/google (replaces MEH-274 cu | Refactor the custom GSI hook (MEH-274) onto @react-oauth/google — explicitly "refactor only, no behaviour change", replacing working code with the community standard. The bug it grew out of is already fixed. Touching Google sign-in is exactly the wrong risk to take into a launch week for a maintenance win, and its regression spec (login console-clean) is the gate either way. |
| **MEH-387** | 🎨 Personalization layer (post-launch) — Editorial homepage + tags + si | Parent of the personalization vision (editorial homepage rotation, tags, similar businesses). Documentation-only by its own header, with four explicit entry conditions of which the site meets none: launch happened, 30 days of real analytics, ≥50 registered businesses, an active newsletter/CRM. The card's own research is the argument for deferring — a DOUBLE cold start (~10 businesses, 0 active users) makes any recommendation random or repetitive. Kept as DEFER rather than MOOT because it is a deliberate scaffold with real downstream work, not a void premise. |
| **MEH-388** | [Sub 1/5] 🏷️ Tags taxonomy + data model — dietary, values, business_ty | Sub 1/5 of MEH-387 and the foundation of the rest — dietary/values/business_type/region tag taxonomy plus admin UI and a RED migration. Post-launch with the parent. Its most important instruction is editorial, not technical: the vocabulary must be settled by Sapir tagging 5-10 real businesses by hand and iterating until the list DISCRIMINATES (if everything is tagged "organic", the tag is worthless) — and there are no real businesses to tag yet. The 09/08 groom confirms MEH-293's product-level dietary flags shipped (models.py:659-666), so this builds on scenario א'. |
| **MEH-389** | [Sub 2/5] 👤 User preferences profile — dietary, values, home_city | Sub 2/5 — user preference fields (dietary, values, home city/coords) plus GET/PATCH endpoints and a settings tab. Hard-blocked by MEH-388: preferences must validate against a vocabulary that does not exist. Post-launch with the parent, and there are no users whose preferences could be stored. |
| **MEH-390** | [Sub 3/5] 🎯 Onboarding mini-quiz — 3 שאלות, אופציונלי, dismissable | Sub 3/5 — a 3-question onboarding quiz. Blocked by both MEH-388 (vocabulary) and MEH-389 (persistence endpoint), post-launch with the parent. Worth carrying forward when it runs: its own UX warning caps it at 3 questions and 5 options with a mandatory skip, on evidence that more than 3-4 simultaneous choices costs up to 60% drop-off — a quiz added to signup before launch would REDUCE registrations, i.e. the opposite of what launch needs. |
| **MEH-413** | 📝 Producer outreach package — sales script + objection handlers + What | Producer outreach package — 10 objection handlers, a 5-step call script, 4 WhatsApp templates, and an objection log. Not code (a Google Doc), and its absence blocks nothing technically. BUT it is the practical enabler of MEH-409, which IS in BLOCKS: it exists because every first conversation currently means improvising answers, with inconsistent replies ("free forever" one day, "we'll see" the next) and no log of which objections recur. RECOMMENDATION: if Sapir is about to start outreach, this is the single most useful DEFER row to do first — it costs a document and directly serves the one card that actually gates launch. |
| **MEH-533** | 🍳 [Post-launch review] Make Eat partnership for home bakers (Persona 3 | Post-launch REVIEW (not build) of a Make Eat partnership — the only legally clean path back to the ~30% of supply that MEH-528 filtered out by requiring a producer licence. Five explicit entry criteria, of which the site meets zero: launch + 12 months + 50 producers + 30 days analytics + stable trust signals. A decision tree for a future conversation with a third party, not work. |
| **MEH-581** | 📊 Static analysis Layer 2: Hybrid annotation upgrade — baseline-vs-cur | Upgrade the three warn-only static-analysis checks (mypy strict / Knip / tsc strict) from `|| true` green-always to a sticky PR comment showing baseline-vs-current deltas. Trigger is explicitly launch + 14 days, and the pre-launch choice of `|| true` was deliberate. Same family as MEH-1868's warn-only ratchet finding — the current state is silent rot rather than a false green — but it is CI ergonomics with zero consumer surface. Note it edits `.github/workflows/**`, which is CC-deny, so it lands as a patch doc for Sapir either way. |
| **MEH-583** | 📊 k6 capacity-ceiling test — disable slowapi for controlled run (post- | k6 capacity-ceiling run with slowapi temporarily disabled, because the MEH-559 baseline measured "the rate limiter works", not the stack's ceiling (95% of responses were 429 after the first 3 seconds). Triple-gated on launch + 30 days, 50 producers and 200 users — and the card's own reasoning is decisive: with zero users capacity is not a constraint, and the setup carries a real risk of its own (forgetting to re-enable slowapi is a security hole). The existing first-3-seconds baseline was explicitly judged sufficient for pre-launch. |
| **MEH-640** | 🔬 Research: Tagline strategy — do we need one? If yes, what + length + | Research on whether the brand needs a tagline at all, and if so which. Frozen by the 09/08 orchestrator ruling, tagged post-launch, and it is a claude.ai research task with no repo file to touch (verified in the groom). It also waits on MEH-637 Phase 1, since the lockup formats decide the tagline's scope. A brand decision that is Sapir's alone (rule 22) and changes nothing a user can hit. |
| **MEH-781** | ✍️ ‏Sapir: ‏copy verbatim package (‏fix-wave DEFER) + ‏retire ל-4 stal | Two paperwork items bundled so they do not get lost: apply Sapir-approved copy verbatim from the fix-wave DEFER package, and retire 4 stale deferred items in HANDOFF.md. Hard-blocked on Sapir supplying the wording (rule 22 — copy is hers, and the prompt forbids improvising). Housekeeping. |
| **MEH-796** | 🔴 Decommission home_products subsystem (Expand-Contract) — router + ad | Decommission the home_products subsystem — router, admin endpoints, 3 models, 6 schemas, the moderation and rating-dispatcher services, the account-deletion cascade, and 3 DB tables. VERIFIED ON THE LIVE TREE, not taken from the card: `app.include_router(home_products.router)` is commented out at router_registry.py:90 under MEH-1406, so the surface is unmounted and its two known security defects (F-1 rating-auth boundary, F-2 BOLA on /whatsapp-click) are unreachable today. WORTH SAYING OUT LOUD ANYWAY: MEH-1739/1740, the fixes for those two, were CANCELLED as "not relevant while the surface is disconnected" — so nothing was repaired, only switched off, and the comment block at :56-90 spells out exactly what re-mounting would take. Anyone re-enabling this router must fix F-1 and F-2 first. The remaining work is dead-code removal plus a RED table drop; no user-facing change either way. |
| **MEH-953** | 📅 Hebrew vector basemap — DEFERRED post-launch; Protomaps self-host (P | Hebrew vector basemap via self-hosted Protomaps. Deferred by Sapir 25/06 on the strength of MEH-954's research, and the reasoning is the good kind: the migration does NOT fix the symptom that started it — Arabic labels across Syria/Jordan are unavoidable with any provider because `name:he` mostly does not exist there — while OSM raster ALREADY renders Hebrew for Israel. So it buys a palette improvement in exchange for a central rewrite, a CSP change, a hosting dependency and ~200KB. Launch ships on OSM raster. |
| **MEH-972** | 📝 Home warmth pass — Sapir signature accent + flat paper-grain ambient | Founder-signature accent, now scoped to a single placement beside Sapir's existing quote on /about/process. Post-launch, frozen 09/08, and hard-blocked on an asset only Sapir can supply (a scan of her real signature for Design to vectorise). Note the scope ruling in the card, which is good brand reasoning: the homepage placement was CANCELLED because a founder signature makes the founder the subject, and both the subject-test (MEH-579) and BRAND.md §1 ("producer = subject of editorial") put the reader and the business there instead. The paper-grain half moved out to MEH-1990. |
| **MEH-974** | 📰 /map region chips — data-gated (live-count area chips on the gateles | Live-count area chips on the gateless /map, deliberately data-gated at ~25+ producers spread over several regions. The card's reason is exactly right and self-defeating to ignore: at ~5 businesses the chips ADVERTISE EMPTINESS ("צפת 1 · כרמיאל 1") and cannot be QA'd. Shipping them at launch would make the catalogue look thinner than it is. The endpoint (GET /producers/cities) already exists and is waiting. |
| **MEH-1687** | 🔎 Design-debt audit — READ-ONLY sweep לפי design-principles.md: ספירת  | READ-ONLY design-debt sweep across the public surfaces, counting eight patterns (action-element radii, emoji in JSX, physical RTL classes, inline hex, sub-44px targets, legacy fonts, glassmorphism, "יצרן" leaking into UI strings) with file:line and a 🔴/🟡/🟢 triage. Its discipline is the right one — COUNT, do not sample, per the MEH-686 lesson that a single symptom hides a systemic gap. But it produces a report, not a fix: nothing changes for a user when it runs, and every finding then needs its own ticket. Cheap and zero-risk (no branch, no commits) if someone wants a map of the debt. |

### MEH-1456 — its own paragraph, as the card requires

**Verdict: DEFER. Recommendation: land chunks 0–2b before launch anyway, and the
single next action is Sapir's, not CC's.** Those are two different questions and
the card asks for both, so they are answered separately here.

**Why DEFER by the criterion.** Categories are keyed on their Hebrew display
name instead of a stable `slug`. No live-site user is harmed by that today —
nothing is misrepresented, no page fails, no data is lost by simply leaving it.
It was considered for BLOCKS and rejected because the foot-gun needs an **admin
action** to fire; it is not spontaneous user harm.

**Why the recommendation still points the other way, and this is the part worth
reading.** The foot-gun is armed *right now*, not hypothetically. MEH-1530
Chunk 1 merged 26/07 15:43Z and made `seed_categories` insert-only, which opened
an exposure window that stays open until **this** card's Chunk 2 lands. Inside
that window, renaming a category in the admin panel produces **two** faults at
once: the rename sticks *and* the canonical name reappears as a second row
beside it, and the renamed row falls through to `DEFAULT` in `category-registry`
and loses its icon (the MEH-1268 pattern). Both cards carry the same operational
note — **do not rename a category in the admin panel** — and that note is free,
costs nothing to honour, and is the cheapest mitigation available today.

Sapir already ruled on the timing on 24/07: chunks 0, 0b, 1, 2 and 2b run
**before** launch; only chunk 3 (re-keying the frontend under a "zero visual
change" contract, during launch QA) is post-launch. The reasoning she approved
is that migration risk is driven by **data volume and live traffic**, both of
which are zero right now and neither of which will ever be lower — after launch
the same migration touches a taxonomy that real businesses are linked to. That
is a cost argument, and it is a good one; it is simply not the same question as
"does a user get harmed at launch", which is what this audit ranks on.

**The gate is a measurement only Sapir can take.** Chunks 0, 1 and 2 are blocked
on measuring **production**'s `categories` table — the five SQL queries written
out in MEH-1530 §2.9, run in the Railway SQL editor. CC cannot do it: all
`*.up.railway.app` egress is blocked from the sandbox and there is no Postgres
MCP. Everything measured so far (holes at ids 1, 5, 13, 15; sequence safe;
zero admin/stale rows) is **staging**, and MEH-1530 records two separate
occasions where a Railway screenshot was nearly attributed to prod by mistake.
The decisive query is the third one: if `id=k` equals the k-th item of
`CATEGORIES` for every k in 1..18, the positional renames already committed on
prod and the live taxonomy is corrupted — which is a materially worse finding
than this card, and nobody knows either way. **Chunk 0b (proving the `emoji`
column dead) is read-only and is not blocked by any of this** — it can run now.

---

## 3 · MOOT

| ID | Title | Evidence |
|---|---|---|
| **MEH-1434** | 🐛 דף הבית — חלל אנכי מת בסקשן "בתי עסק מקומיים" (retargeted מ-/produce | PREMISE MEASURED AND REFUTED — this is the failing precondition, named precisely: the card assumes a reproducible dead vertical band caused by grid cards stuck at opacity:0 because whileInView never fires. Phase 0 (13/08) built a probe against a real Chromium with 8 synthetic producers and measured the opposite at BOTH viewports — 1440x900 and 375x812 — 8 of 8 cards transition opacity 0 to 1 after scroll+settle, correctly positioned. The counter-to-grid gap is a consistent 70px, far smaller than any meaningful band. The suspect does not exist. What is NOT refuted is that Sapir saw something real in the original crop; the residue is a possible transient frame (category d), which the card's own DoD says is a STOP, not an edit. |
| **MEH-130** | 🗺️ מהמקור Roadmap — סדר עבודה מלא (v2) | PREMISE VOID, and the specific failing precondition is nameable: as a launch-blocker inventory the card's content no longer refers to anything. The 09/08 groom verified it against Linear directly — four of the issues its layers 1-2 name as "blocking everything" (MEH-124, MEH-76, MEH-131, MEH-408) return `Could not find referenced Issue`, MEH-136 has been Done since 11/06, and the work those layers describe was in fact delivered through entirely different cards (MEH-1136/1146 for ProducerDetail, MEH-964 for the dashboard). Its critical path was written in the MEH-100-500 range; the board is now past MEH-2900. What survives is the philosophy, the 7 rules and the parallelism map — a live reference document, which is not work and cannot block a launch. ⚠️ SEPARATE OPERATIONAL NOTE: it sits at **Urgent** priority in Backlog, which is the highest priority on the board and belongs to a document. Any priority-ordered triage will surface it first. |
| **MEH-174** | 🚀 Epic — הקמת Claude Cowork כ-operations hub למהמקור | PREMISE VOID — the specific missing precondition is that the Epic's four children do not exist. The 09/08 groom searched `parentId=MEH-174` including archived, searched "Cowork" workspace-wide including archived, and queried each quoted UUID: MEH-175/176/177/178 all return `Could not find referenced Issue`. Its DoD is "all 4 sub-issues Done", so there is literally nothing to track it against. NOT proof Cowork was never set up — Cowork lives outside the repo — only that the Epic as written is untrackable. Needs a Sapir decision (restore children / rescope / close), not engineering. Zero consumer surface either way. |

---

## 4 · FLAGGED — could not classify without a query, production data, or a Sapir decision

Not guessed into a bucket, per the card's own instruction.

| ID | Title | Evidence |
|---|---|---|
| **MEH-2020** | 🔤 decision-first — איזה charset מותר ב-slug ציבורי? היום ערבית/קירילית | CANNOT CLASSIFY without a test I did not run. Measured behaviour is real: _slugify uses Python 3 \w which is Unicode-aware, so Arabic/Cyrillic/CJK slugs are served in production today by regex default, not by decision. The charset policy itself is a Sapir product decision (DEFER-shaped). BUT the card records an UNVERIFIED homograph vector against RESERVED_SLUGS — if a look-alike character can claim a reserved path, that is security and BLOCKS. TO RESOLVE: test whether the RESERVED_SLUGS comparison is homograph-safe. Not guessed into a bucket. |
| **MEH-2069** | ♿ שני מופעי אחות ל-MEH-2032 — bg-accent/10 + text-accent על טקסט קטן ב | CANNOT CLASSIFY from the card — and the card itself says so. The sibling (MEH-2032) measured bg-accent/10 + text-accent at 4.07:1, an AA failure, but ONLY when that chip sits on the cream #f5f0e8 background. Whether EventsClient.jsx:425/467 and AboutProcessClient.jsx:96 render on cream or on white (#ffffff) is explicitly unverified, and on white the number changes and may pass. TO RESOLVE: measure the computed background behind each chip in a real DOM. If cream → AA failure → IS 5568 / Israeli accessibility law is regulatory exposure and this is BLOCKS. If white → close with no code change. |

---

## 5 · Discovered along the way

Findings surfaced by the sweep that are **not** one of the 175 cards. Per this
card's scope, none of these was opened as a Linear issue — they are listed for
Sapir to decide on.

| Surfaced by | Finding |
|---|---|
| MEH-1514 | Founder portrait (AboutClient.jsx:166, IMG-01) renders as a solid MAGENTA block in every /about VRT baseline, including human-reviewed ones. `onError` did not fire, so this is NOT the coded fallback path. Whether it also happens on the LIVE site was never checked. If it does, it is a product bug on a consumer page, not a test-environment artifact. Not opened as a card (this audit does not open cards) — needs one look at live /about. |
| the sweep | **Part of the 175 is not work.** At least 4 open Backlog cards (MEH-2054, MEH-2061, MEH-2066, MEH-2074) are spent "Carrier" tickets: they exist only so a docs-only session log can satisfy the `Branch name gate` (which requires `meh-NNNN` in every legal branch slug), and every one of their PRs (#2869, #2883, #2893, #2917) is already merged. Closing them costs nothing and shrinks the backlog's apparent size. Root cause is the same gate-vs-auto-link tension tracked by MEH-1736/1615/1949 — if the branch-name auto-link is turned off at workspace level, carrier tickets stop being necessary at all. |
| MEH-1456 / MEH-1530 | **A Done, archived card is holding an open pre-launch blocker, and nothing surfaces it.** MEH-1530 was auto-closed by the GitHub integration 2 seconds after PR #2138 merged (26/07 15:43:21Z) — its own §10.7 says so plainly: "merge closes the code, not the verification". Three of its DoD items are still unticked, all of them observed effects on staging (zero-row delta, `[bg 2/2] seed OK` in the Railway log, `/health/readiness` → 200), plus the **production `categories` measurement** (five queries, §2.9) that blocks MEH-1456 chunks 0/1/2 — the pre-launch work Sapir approved on 24/07. A Done card is invisible to every queue query, so this blocker is parked where nobody will find it. Not opened as a card (this audit does not open cards); it belongs either on MEH-1456 as an explicit dependency line or reopened on MEH-1530. |
