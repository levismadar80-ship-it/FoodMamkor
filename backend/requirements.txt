fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
psycopg2-binary==2.9.9
alembic==1.13.2
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-multipart==0.0.9
pydantic[email]==2.9.2
pydantic-settings==2.5.2
cloudinary==1.40.0
google-auth==2.34.0
twilio==9.3.0
PyJWT[crypto]==2.9.0
requests==2.32.3
openpyxl==3.1.5
anthropic==0.40.0
# httpx is a transitive dep of anthropic. Pin a floor to ensure pip
# resolves to a version that anthropic 0.40 actually supports — older
# anthropic (0.39) called the underlying httpx Client with `proxies=`
# (plural, the pre-0.28 API), and httpx 0.28 dropped that kwarg in
# favor of `proxy=`. Result: TypeError at client construction. Bumping
# anthropic to 0.40 fixes the SDK side; this floor exists as belt-and-
# suspenders + so the next person who reads this file knows the two
# packages are coupled.
httpx>=0.27.0
slowapi==0.1.9
